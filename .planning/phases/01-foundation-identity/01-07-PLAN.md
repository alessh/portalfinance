---
phase: 01-foundation-identity
plan: 07
type: execute
wave: 5
gap_closure: true
depends_on: [01-00, 01-01, 01-02, 01-03, 01-04]
files_modified:
  - docs/ops/ses-production-access.md
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "AWS Support's denial of SES production access (received 2026-05-01) is captured verbatim in docs/ops/ses-production-access.md so future ops decisions cite the actual denial reason rather than a paraphrase."
    - "Three follow-up paths are documented with concrete next-actions: (1) appeal the denial with revised justification (DEFAULT — preserves locked decision D-09 'AWS SES sa-east-1 is the transactional email provider for Phase 1 and forward' + BR-residency posture), (2) provision a separate AWS account in the org and re-request, (3) pivot to an alternative ESP (Postmark / Resend / SendGrid) — Path 3 explicitly REQUIRES re-opening D-09 with a fresh ADR before adoption. Each option lists effort, BR-residency posture, and minimum integration delta from current SES code."
    - "STATE.md Deferred Items table is updated to reflect 'SES production access — DENIED 2026-05-01' (recorded by the executor via gsd-sdk handler since planners do not edit STATE.md directly per repo convention)."
    - "This plan closes the third-party block recorded in HUMAN-UAT Test 1 — Phase 02 (Pluggy ingestion) is no longer waiting on a code-side outcome from this plan."
  artifacts:
    - path: "docs/ops/ses-production-access.md"
      provides: "Updated runbook including Section 8 (denial record) + Section 9 (alternative ESP comparison) + Section 10 (decision log)"
      contains: "DENIED 2026-05-01"
  key_links:
    - from: "HUMAN-UAT Test 1 (blocked_by: third-party)"
      to: "docs/ops/ses-production-access.md"
      via: "Verbatim denial text + remediation paths recorded for ops"
      pattern: "AWS Support"
    - from: "STATE.md Deferred Items"
      to: "docs/ops/ses-production-access.md"
      via: "Deferred Items entry updated to reflect denied status"
      pattern: "SES production access"
---

<objective>
HUMAN-UAT Test 1 (SES email delivery end-to-end) is `blocked_by: third-party`: AWS
Support denied the SES production-access request on 2026-05-01 without specific
remediation guidance. This is NOT a code defect — it is an operational decision
gate. The user has indicated readiness to start Phase 02 (Pluggy ingestion), so
this plan must NOT block on the SES decision. Instead, it captures the denial,
documents the three viable follow-up paths, and updates the project state so the
next ops session has a clear menu of choices without re-reading the original
denial email.

Purpose: Convert an unstructured operational blocker into a structured ops runbook
entry, so a future Claude/operator pair can choose a path (appeal, new account, or
ESP pivot) with full context. Specifically:
- Capture the denial verbatim so future appeals/escalations cite exact wording.
- Compare three remediation paths side-by-side with BR-residency posture, code
  delta, and effort estimate so the decision is informed.
- Record the decision log timestamp pattern future runbook entries should follow.

**(W1) Locked decision D-09 governs the framing of Section 9.** Per
`.planning/phases/01-foundation-identity/01-CONTEXT.md`:

> **D-09:** **AWS SES `sa-east-1`** is the transactional email provider for Phase 1
> and forward. Strongest LGPD residency story (matches Pluggy/ASAAS/Sentry-EU
> pattern). Used for password-reset, account-unlock, DSR acknowledgment, and later
> Phase 2 re-auth + Phase 5 billing notifications.

D-09 is locked. Section 9 must therefore frame **Path A (Appeal)** as the
default-if-no-decision option (preserves D-09 + BR-residency posture). **Path C
(ESP Pivot — Postmark/Resend/SendGrid) is documented as an alternative that
EXPLICITLY REQUIRES re-opening D-09 with a fresh ADR before adoption.** All
factual content about Postmark/Resend/SendGrid stays — but it stops being framed
as "default recommendation for v1." The recommendation matrix's "Recommended for
v1" column reflects the D-09 lock. Any verdicts that cast Path C as the
preferred near-term path move into a clearly-titled subsection ("If ops chooses
to revisit D-09…") so the framing cannot be misread by a future planner.

Output: Updated `docs/ops/ses-production-access.md` with three new sections (8, 9,
10) appended to the existing runbook. STATE.md Deferred Items entry updated by the
executor via the `gsd-sdk` state mutation handler.

This is a **docs-only plan**. No source code is modified. No tests are added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-HUMAN-UAT.md
@./CLAUDE.md

@docs/ops/ses-production-access.md
@docs/ops/aws-copilot-setup.md

<denial_text>
<!-- Verbatim denial text — provided by HUMAN-UAT Test 1 reason field, normalized to a single paragraph: -->
"we are unable to approve an increase at this time... Due to security reasons, we
are unable to provide specific details about our assessment criteria." Pointed to
the AWS Acceptable Use Policy and Service Terms.
</denial_text>

<context_constraints>
- **D-09 (LOCKED):** AWS SES `sa-east-1` is the transactional email provider for
  Phase 1 and forward. Adopting any non-SES ESP requires re-opening D-09 with a
  fresh ADR — this is NOT a planner-level decision. Section 9 must reflect this.
- Sender domain MUST be hosted in `sa-east-1` per LGPD residency posture (see PROJECT.md "BR data residency" + STACK.md "Disqualified for BR residency").
- Postmark, Resend, and SendGrid all support `From:` addresses on customer-controlled domains regardless of where their infrastructure sits — the residency concern is about data IN TRANSIT and AT REST in the ESP. Postmark/Resend store delivery metadata in US/EU; SendGrid US/EU. **None of these store BR-resident user data**: the `to` address is the only PII the ESP sees, and we hash that in our logs already (see plan 01-05). The LGPD posture WOULD become "transactional emails are processed via a third-party ESP whose role is documented in the privacy policy" — same as Pluggy/ASAAS/Sentry-EU — IF and only if D-09 is reopened and the new ESP is approved.
- Code-side abstraction: `src/lib/mailer.ts` is a thin SES wrapper. Switching providers means swapping the `SendEmailCommand` call for the new ESP's SDK call. The interface (`sendEmail(params: SendEmailParams): Promise<SendEmailResult>`) stays stable — workers and routes do not change. This makes the eventual "if D-09 is reopened" pivot cheap, but does NOT itself authorize the pivot.
</context_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append denial record + remediation paths (D-09 framing) + decision log to ses-production-access.md</name>
  <read_first>
    - docs/ops/ses-production-access.md (entire file — you are appending to the bottom)
    - .planning/phases/01-foundation-identity/01-HUMAN-UAT.md (the verbatim denial text in Test 1 `reason` field)
    - .planning/phases/01-foundation-identity/01-CONTEXT.md (D-09 — the locked decision that governs framing)
    - .planning/STATE.md (Deferred Items table — to know what entry shape to update in Task 2)
    - src/lib/mailer.ts (review only — to scope the "code delta" estimate accurately for each ESP option; do NOT modify)
  </read_first>
  <action>
1. **Append to `docs/ops/ses-production-access.md`** (do NOT delete or modify existing sections 1–7). Add three new sections at the end of the file:

   **Section 8 — Denial Record (2026-05-01)** — verbatim denial:
   ```markdown
   ---

   ## Section 8 — Production Access Denial (2026-05-01)

   **Status:** DENIED
   **Decided:** 2026-05-01
   **Decided by:** AWS Support
   **Account:** Portal Finance production AWS account (sa-east-1 SES sandbox)
   **Channel:** AWS Support case response

   ### Verbatim denial

   > "we are unable to approve an increase at this time... Due to security reasons,
   > we are unable to provide specific details about our assessment criteria."

   AWS Support pointed to the AWS Acceptable Use Policy and the AWS Service Terms.
   No specific remediation guidance was provided. The denial does not name any
   policy violation; AWS explicitly declined to disclose assessment criteria.

   ### Operational impact

   - SES remains in sandbox mode in `sa-east-1` for the Portal Finance prod account.
   - Sends to non-verified destinations fail silently from the user's perspective
     (the bounce-simulator address `bounce@simulator.amazonses.com` produced no
     observable outcome from the deployed prod env on 2026-05-01 — see HUMAN-UAT
     Test 4 / blocker 2; observability was independently fixed by plan 01-05).
   - Sends to verified destinations (e.g., the developer's personal address)
     continue to work — confirmed 2026-05-01.
   - **Phase 02 (Pluggy ingestion) is NOT blocked** by this decision — Pluggy
     onboarding does not depend on transactional email delivery to arbitrary users.
     The block becomes critical at the first user-facing email touchpoint that
     reaches non-verified addresses (Phase 5 paid signup at the latest, but in
     practice the password-reset and account-unlock flows already exercise this
     path during user testing).
   ```

   **Section 9 — Remediation Paths** — three side-by-side options, framed against
   locked decision D-09:
   ```markdown
   ---

   ## Section 9 — Remediation Paths

   > **Locked decision D-09 (CONTEXT.md, Phase 01):** _AWS SES `sa-east-1` is the
   > transactional email provider for Phase 1 and forward._ Path A and Path B
   > below preserve this decision. **Path C (alternative ESP) requires re-opening
   > D-09 with a fresh ADR before adoption — it is NOT a near-term default and
   > MUST NOT be adopted without an explicit decision-reversal process.** The
   > content of Path C is documented because the eventual cost of a pivot is low
   > thanks to the thin `src/lib/mailer.ts` abstraction, and ops should know what
   > a pivot would cost if it is ever reopened. It is recorded here as
   > **information**, not as a recommendation.

   Three paths, in priority order. Each lists effort (S/M/L), residency posture,
   and the code delta from the current SES wrapper at `src/lib/mailer.ts`.

   ### Path A — Appeal the denial with revised justification (DEFAULT)

   - **Effort:** S (1-2 hours of writing + 24-72h AWS review)
   - **Residency:** unchanged (sa-east-1 SES) — preserves D-09
   - **Code delta:** none
   - **Decision impact:** none — D-09 unchanged
   - **Steps:**
     1. Open a new AWS Support case under the same account (case type
        "Technical → SES Sending Limit Increase").
     2. Reference the prior case ID and acknowledge the denial.
     3. Provide additional evidence: live `ses_suppressions` count = 0 (clean
        history), DKIM/SPF/DMARC verified at apex (see Section 1), bounce/complaint
        webhook proven by `tests/integration/webhooks/ses-bounce.test.ts`,
        suppression-guard proven by plan 01-05 mailer tests, structured logging
        proven by plan 01-05.
     4. Provide expected first-month volume (~500 messages, mostly password reset)
        — much lower than the original ask of 5 000.
     5. Provide a contact phone number for AWS to verify legitimacy.
   - **Risk:** AWS may deny again with the same boilerplate. No SLA on appeal.
   - **Default-if-no-decision:** YES. If ops takes no action, this is the path
     that aligns with D-09 and should be pursued first.

   ### Path B — Provision a separate AWS account in the organization

   - **Effort:** M (1 day to set up + 24-72h SES approval + Copilot env wiring)
   - **Residency:** unchanged (sa-east-1 SES in a different AWS account) — preserves D-09
   - **Code delta:** none — but Copilot manifests reference the current account's
     IAM task role; a cross-account move requires re-running `copilot env init`
     against the new account or using cross-account SES sending (more complex).
   - **Decision impact:** none — D-09 unchanged (still SES sa-east-1)
   - **Steps:**
     1. Create a new AWS account under AWS Organizations: `portalfinance-email`
        with billing under the master account.
     2. Verify domain identity for `portalfinance.app` in the new account
        (matches Section 1 steps).
     3. Request production access from the new account (Section 2).
     4. Cross-account SES: grant the prod ECS task role permission to assume an
        SES role in the new account, attach `ses:SendEmail` on the target.
        Modify `src/lib/mailer.ts` to use STS assume-role. **OR**: keep prod ECS
        in the original account and simply move all SES operations to the new
        account by attaching identity-based credentials via SSM (back to a
        long-lived key, which violates D-17 / SEC-02).
   - **Risk:** Cross-account IAM is fragile; the simpler path (long-lived keys)
     violates the no-static-credentials posture established in plan 01.1-03.

   ### Path C — Pivot to an alternative ESP (REQUIRES D-09 REOPEN)

   > **⚠ Decision-reversal required.** Adopting any option in Path C requires:
   > (1) opening a new ADR proposing to retire D-09; (2) the ADR documenting the
   > LGPD-residency justification for the new ESP's data-handling location;
   > (3) explicit user (developer) approval before code changes ship; (4) updating
   > CONTEXT.md and PROJECT.md "Disqualified for BR residency" lists if the new
   > ESP currently appears there. Until those four steps complete, Path C is
   > documented but NOT actionable.

   Three candidates are catalogued below for completeness — so that IF D-09 is
   reopened, ops has the comparison ready and does not need to re-research.

   #### C.1 — Postmark
   - **Effort:** S (2-3 hours of code work) — plus the D-09 reopen process before
     any code ships
   - **Residency:** Delivery infra US (Postmark) + EU (mirror); customer data
     stored 45 days. From an LGPD perspective: equivalent to current Sentry-EU
     posture — third-party processor disclosed in privacy policy with DPA. Note:
     this posture is currently DISQUALIFIED by the locked stack (`STACK.md`)
     until D-09 is reopened.
   - **Code delta:** Replace `@aws-sdk/client-ses` with `postmark` SDK in
     `src/lib/mailer.ts` (~30 lines changed). The `sendEmail` interface stays
     stable; the worker / route layers do NOT change. Replace `SendEmailCommand`
     with `client.sendEmail({ From, To, Subject, HtmlBody, TextBody, MessageStream: 'outbound' })`.
   - **Bounce handling:** Postmark exposes inbound webhook for bounces — replace
     the SNS-receiver in `src/app/api/webhooks/ses/bounces/route.ts` with a
     Postmark-shaped webhook. Idempotency on `MessageID` (existing
     `webhook_events` table works unchanged).
   - **Cost:** ~US$15/mo for 10 000 emails (well within budget).
   - **Sandbox/prod parity:** Postmark has no sandbox concept — production from
     day 1 with a sender-signature verification flow (~5 minutes).

   #### C.2 — Resend
   - **Effort:** S (2-3 hours of code work) — plus the D-09 reopen process
   - **Residency:** Delivery infra US; EU residency available on Pro plan. For
     LGPD posture, Pro plan + EU region is equivalent to Postmark/Sentry-EU.
   - **Code delta:** Replace SES SDK with `resend` SDK (~30 lines). Resend has
     first-class React Email integration (`resend.emails.send({ react: <Template/> })`)
     which would let `src/lib/mailer.ts` skip the explicit `render()` call.
   - **Bounce handling:** Resend webhook on `email.bounced` / `email.complained`.
   - **Cost:** Free tier 3 000 emails/mo; ~US$20/mo for 50 000.
   - **Sandbox/prod parity:** Sandbox = test addresses only (delivered@resend.dev,
     bounced@resend.dev) — pattern-equivalent to SES simulator.

   #### C.3 — SendGrid
   - **Effort:** M (3-4 hours of code work — more boilerplate than Postmark/Resend)
     — plus the D-09 reopen process
   - **Residency:** US-only by default; EU requires Marketing Campaigns plan
     (overkill for transactional). For LGPD posture, equivalent to Postmark.
   - **Code delta:** Replace SES SDK with `@sendgrid/mail` (~40 lines). API is
     more complex (categories, custom args, separate Subusers concept).
   - **Bounce handling:** SendGrid Event Webhook with HMAC verification.
   - **Cost:** Free tier 100 emails/day; ~US$20/mo for 50 000.

   ### Recommendation matrix

   | Criterion              | Path A (Appeal) | Path B (New Account) | Path C.1 Postmark | Path C.2 Resend | Path C.3 SendGrid |
   |------------------------|-----------------|----------------------|-------------------|-----------------|-------------------|
   | Time-to-unblock        | 1-3 days        | 2-5 days             | half day*         | half day*       | half day*         |
   | Code change            | none            | minimal              | ~30 LoC           | ~30 LoC         | ~40 LoC           |
   | LGPD residency posture | sa-east-1       | sa-east-1            | EU mirror*        | EU on Pro*      | US*               |
   | Risk of repeat block   | high            | medium               | none              | none            | none              |
   | D-09 status            | preserved       | preserved            | requires reopen   | requires reopen | requires reopen   |
   | Default-if-no-decision | **YES**         | no                   | no                | no              | no                |

   *Path C entries: cited code/time costs are AFTER the D-09 reopen completes.
   The decision-reversal effort itself (ADR + user approval + STACK.md / PROJECT.md
   updates) is NOT included in those numbers and typically takes 1-3 hours of
   writing plus user-decision turnaround.

   ### Default action if ops needs to unblock today

   **Pursue Path A (Appeal).** It preserves D-09, requires no code, and matches
   the locked LGPD residency posture. Path B is the second-priority fallback if
   the appeal is denied a second time and ops still wants to keep SES.

   ### If ops chooses to revisit D-09…

   _This subsection exists only because the ESP-pivot path (C) is sometimes
   raised in operational discussions. Adoption of any C-path option is gated on
   the four-step decision-reversal process above. If ops decides to pursue
   reversal:_

   1. _Open a new ADR ("D-09-rev1: revisit transactional email provider") with the
      LGPD-residency analysis for the proposed ESP._
   2. _Get explicit developer/user approval recorded in `.planning/STATE.md`._
   3. _Update `STACK.md` "Disqualified for BR residency" entries (Postmark / Resend
      / SendGrid currently appear there)._
   4. _Then plan the code change (separate plan, separate review). The
      recommendation among C.1/C.2/C.3 — IF a pivot were ever approved — would
      tilt toward Postmark on grounds of simplicity + EU mirror + transparent
      pricing. **This is not a current recommendation.**_
   ```

   **Section 10 — Decision Log** — chronological table:
   ```markdown
   ---

   ## Section 10 — Decision Log

   | Date       | Action                                  | Outcome                                          | Decided by         |
   |------------|-----------------------------------------|--------------------------------------------------|--------------------|
   | 2026-04-22 | Phase 01-04 Task 4 requested SES prod access (justification per Section 2 verbatim block) | Pending AWS review | Plan 01-04 / ops   |
   | 2026-05-01 | AWS denied production access            | Recorded in Section 8                            | AWS Support        |
   | 2026-05-01 | Plan 01-07 documented denial + remediation paths (D-09 preserved as default; Path C documented as alternative requiring decision reversal) | Awaiting ops decision: Path A appeal vs Path B new-account | Phase 01 gap closure |

   Future entries: append a new row when ops chooses a remediation path. Reference
   the decided path's section letter (A/B). If a Path C option is ever pursued,
   the row MUST cite the D-09 reversal ADR ID.
   ```

2. **Constraints:**
   - DO NOT delete or modify any of the existing sections 1-7 in the file.
   - DO NOT modify any source code files (this is a docs-only plan).
   - DO NOT create new files outside `docs/ops/ses-production-access.md`.
   - DO use the verbatim denial wording exactly as quoted (do not paraphrase).
   - DO use US-English per repo convention (CLAUDE.md).
   - DO frame Path A as the default and Path C as decision-reversal-gated (W1) —
     do NOT use language like "default recommendation" or "Recommended for v1: high"
     for any Path C entry. The recommendation matrix's "D-09 status" column is
     load-bearing for this framing.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('docs/ops/ses-production-access.md','utf8'); const checks=[/Section 8 — Production Access Denial \(2026-05-01\)/, /DENIED/, /Section 9 — Remediation Paths/, /Path A — Appeal the denial.*\(DEFAULT\)/, /Path B — Provision a separate AWS account/, /Path C — Pivot to an alternative ESP \(REQUIRES D-09 REOPEN\)/, /Postmark/, /Resend/, /SendGrid/, /Section 10 — Decision Log/, /AWS Support/, /D-09/, /requires reopen/, /Default action.*Path A/]; const missing=checks.filter(re=>!re.test(c)); if(missing.length){console.error('Missing:',missing.map(String));process.exit(1);} console.log('OK: all required sections + D-09 framing present');"</automated>
    Manual confirmation:
    - The denial verbatim quote in Section 8 matches HUMAN-UAT Test 1 `reason:` field word-for-word (excluding pipe and quote characters used by YAML).
    - The Recommendation Matrix table's "D-09 status" column shows "preserved" for A/B and "requires reopen" for all three Path C entries.
    - The "Default-if-no-decision" row has YES only for Path A.
    - There is NO standalone "Recommended for v1: high" verdict on any Path C entry.
  </verify>
  <done>
    docs/ops/ses-production-access.md gains three sections (8, 9, 10) without modifying sections 1-7. The denial is captured verbatim, three remediation paths are documented with effort + residency + code-delta estimates, and the decision log records the timeline. Path A is framed as the default that preserves locked decision D-09; Path C is documented as an alternative gated on decision reversal. The file is committable as a single doc commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md Deferred Items table to reflect SES denial</name>
  <read_first>
    - .planning/STATE.md (entire file — focus on the Deferred Items table around line 122-126)
    - docs/ops/ses-production-access.md (the version updated by Task 1 — confirms Section 8 exists for cross-reference)
  </read_first>
  <action>
1. **Update the Deferred Items table in `.planning/STATE.md`** via the `gsd-sdk` state mutation handler. The current row reads:
   ```
   | Ops      | SES production access (24-48h AWS approval) + Sentry EU project (DSN must end with de.sentry.io) — runbooks at `docs/ops/ses-production-access.md`. SES wiring uses IAM task role per workload addon (D-17 / SEC-02). | Open | 01-04 |
   ```

   Run:
   ```bash
   gsd-sdk query state.update-deferred --id "ses-production-access" --status "DENIED 2026-05-01" --note "AWS Support denied 2026-05-01 without specific remediation guidance. Three remediation paths documented in docs/ops/ses-production-access.md Section 9. Path A (Appeal) is the default — preserves locked decision D-09 (AWS SES sa-east-1). Path C (ESP pivot) requires reopening D-09. Plan 01-07 captured the denial. Phase 02 is NOT blocked."
   ```

   If the `state.update-deferred` handler does not exist in the SDK (verify by running `gsd-sdk query --list-handlers | grep deferred`), fall back to direct edit of `.planning/STATE.md`:
   - Replace the existing row with:
     ```
     | Ops      | SES production access — **DENIED 2026-05-01** by AWS Support. Default path: Appeal (preserves D-09). Three remediation paths (Appeal / New AWS Account / ESP Pivot — last requires D-09 reversal) documented in `docs/ops/ses-production-access.md` Section 9. Phase 02 is NOT blocked. | Denied 2026-05-01 | 01-04 / closed by 01-07 docs |
     ```
   - Update the `last_activity` field at the top of STATE.md to: `2026-05-01 -- Phase 01 gap-closure plans 01-05/01-06/01-07 created; SES production-access denial recorded`
   - Update the `last_updated` field at the top of the frontmatter to: `2026-05-01`

2. **Constraints (I4):**
   - DO NOT change progress counters, current phase, milestone, or any field other
     than: (a) the SES Deferred Items row, (b) `last_activity`, and (c) `last_updated`
     at the top of the frontmatter. The three fields listed in (a)-(c) are the
     ONLY fields this task is allowed to mutate. Any other field change must come
     from a separate, dedicated commit/plan.
   - DO NOT add new Deferred Items rows — only update the existing SES row.
   - DO use the verbatim "DENIED 2026-05-01" status string for grep parity with future audits.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/STATE.md','utf8'); if(!/DENIED 2026-05-01/.test(c)){console.error('STATE.md missing DENIED 2026-05-01');process.exit(1);} if(!/01-07/.test(c) && !/closed by 01-07/.test(c)){console.error('STATE.md missing 01-07 reference');process.exit(1);} console.log('OK: STATE.md updated');"</automated>
    Manual confirmation:
    - The Deferred Items table still has the same number of rows (no rows added or removed).
    - The row's `Status` column reads `DENIED 2026-05-01` (or equivalent surfaced by the SDK handler).
    - Only `last_activity`, `last_updated`, and the SES Deferred Items row changed compared to the prior commit (`git diff .planning/STATE.md` shows no other field touched).
  </verify>
  <done>
    .planning/STATE.md Deferred Items reflects the denial. The row preserves the historical context (24-48h approval expectation) but updates the status. Future planning sessions reading STATE.md will see the denial without needing to open HUMAN-UAT.md. No fields other than `last_activity`, `last_updated`, and the SES Deferred Items row were modified.
  </done>
</task>

</tasks>

<verification>
- `docs/ops/ses-production-access.md` contains Sections 8, 9, 10 with the required content (verified by the regex check in Task 1's automated step), Path A is the default, Path C explicitly requires D-09 reopen.
- `.planning/STATE.md` Deferred Items table reflects the denial (verified by the regex check in Task 2's automated step), and `git diff` shows only `last_activity` + `last_updated` + the SES row changed.
- No source code files modified (run `git diff --name-only -- src/ tests/` and confirm zero output after Task 1+2 are complete — this should be a docs-only commit).
</verification>

<success_criteria>
- Two markdown files updated: `docs/ops/ses-production-access.md` (sections 8-10 appended) + `.planning/STATE.md` (Deferred Items row + last_activity + last_updated only).
- Zero source-code files modified.
- Verbatim denial quote present and matches HUMAN-UAT Test 1.
- Three remediation paths fully documented with effort/residency/code-delta — ops can pick a path without re-research. Path A is the default-preserving-D-09 option; Path C requires the documented decision-reversal process.
- Phase 02 is unblocked by this plan (no code dependency on SES).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-07-SUMMARY.md` recording:
- Files modified (2 markdown files only)
- Confirmation that no source code was changed (`git diff --stat` showing only docs/ + .planning/)
- The current operational decision pending: Path A (default — appeal SES) vs Path B (new AWS account). Path C requires D-09 reopen and is NOT a current option.
- Pointer for the next ops session: read Section 9 of `docs/ops/ses-production-access.md` and choose between Path A and Path B; do NOT pursue Path C without first reopening D-09 via ADR.
- Confirmation that Phase 02 (Pluggy ingestion) is NOT blocked by this third-party item.
</output>
