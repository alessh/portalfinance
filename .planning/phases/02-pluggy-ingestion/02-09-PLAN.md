---
phase: 02-pluggy-ingestion
plan: 09
type: execute
wave: 8
depends_on: [02-07, 02-08]
gap_closure: true
files_modified:
  - vitest.config.ts
  - tests/fixtures/db.ts
  - tests/fixtures/integration-globals.ts
  - docs/ops/integration-tests.md
autonomous: true
requirements: [CONN-02, TX-01, CONN-07]
tags: [testing, vitest, testcontainers, infrastructure, gap-closure]

must_haves:
  truths:
    - "vitest.config.ts integration project pins `pool: 'forks'` with `poolOptions.forks.singleFork: true` and `fileParallelism: false`, so all 22 integration suites run sequentially in ONE forked worker process"
    - "vitest.config.ts integration project registers a globalSetup file that boots ONE shared Postgres testcontainer for the entire integration run and stops it on teardown"
    - "tests/fixtures/db.ts startTestDb() returns a singleton — repeated calls within the same process reuse the already-started container instead of spawning a new one"
    - "Running `npm run test:integration` from a clean state (no leaked containers) completes within 8 minutes wall time on Docker Desktop / WSL2 with NO `Hook timed out in 180000ms`, NO `Cannot read properties of undefined (reading 'stop')`, NO `Cannot read properties of undefined (reading 'end')`"
    - "Each integration suite (idor, pluggy/webhook, FaturaDetector, TransferDetector, encryption, free-tier, cooldown, reconcile, sync-worker, reauth-flow, reauth-notifier, disconnect, connect-init, connect-token, db/migrations, db/users-schema, lgpd/consent, lgpd/dsr, ses-bounce, auth/rate-limit, _scaffold, env-assert) executes its own `it(...)` blocks — pass or fail on real assertions, NOT on afterAll cascade TypeError"
    - "docs/ops/integration-tests.md (new) documents Docker prerequisites, the singleton container model, leaked-container cleanup, and the expected wall-time ceiling"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Integration project pinned to single-fork sequential execution + globalSetup hook"
      contains: "singleFork: true"
      contains: "fileParallelism: false"
      contains: "globalSetup"
    - path: "tests/fixtures/db.ts"
      provides: "Module-level singleton testcontainer; startTestDb() returns the same container on repeat calls within a single process"
    - path: "tests/fixtures/integration-globals.ts"
      provides: "Vitest globalSetup — boots one Postgres testcontainer, exposes DATABASE_URL via env, stops on teardown"
      min_lines: 30
    - path: "docs/ops/integration-tests.md"
      provides: "Runbook — Docker prereqs, leaked-container cleanup, expected wall time, troubleshooting"
      min_lines: 40
  key_links:
    - from: "vitest.config.ts integration project"
      to: "tests/fixtures/integration-globals.ts"
      via: "test.workspace[1].test.globalSetup field"
      pattern: "globalSetup"
    - from: "tests/fixtures/db.ts startTestDb()"
      to: "module-level cached PostgreSqlContainer"
      via: "lazy singleton — first caller starts, subsequent callers reuse"
      pattern: "let _cached"
---

<preamble>
## Gap closure plan — Gap 2 (integration test suite cannot run)

**Sources:**
- `.planning/phases/02-pluggy-ingestion/02-HUMAN-UAT.md` Gap 2 (BLOCKER, test 2)
- `.planning/debug/integration-tests-testcontainers-cascade.md` Root Cause 1

**Premise:** All 22 integration test files call `startTestDb()` in their own `beforeAll`, each spinning up a fresh `PostgreSqlContainer('postgres:16-alpine')`. The `vitest.config.ts` integration project does NOT pin pool/parallelism, so Vitest 3.0.5's default file parallelism races ~8 worker processes for Docker simultaneously. On Windows + Docker Desktop / WSL2, parallel container init thrashes the daemon: per-container two-stage init balloons from ~48s (uncontended) to >120s, and the slowest suite (`idor`) blows past its 180s `hookTimeout`. `td` and `pg` are never assigned, and `afterAll` cascades into `TypeError: Cannot read properties of undefined (reading 'stop')` / `(reading 'end')` across 22 files.

**Two complementary fixes.** Apply both — they are belt-and-suspenders for two independent failure modes:

1. **Pool pinning** (cheap, immediate): set `pool: 'forks'` with `singleFork: true` and `fileParallelism: false` on the integration project. All 22 suites run sequentially in one forked worker. This alone would fix the cascade (no concurrent Docker calls), but each suite still calls `startTestDb()` and gets its OWN container — that is wasteful (~48s × 22 ≈ 18 minutes wall time minimum, before any assertions).

2. **Singleton + globalSetup**: refactor `tests/fixtures/db.ts` to memoize the container at module scope, and register a vitest `globalSetup` file (`tests/fixtures/integration-globals.ts`) that boots ONE container before any suite runs and stops it on teardown. With `singleFork: true`, the module is loaded once per process, so the singleton survives across all suites in that process — total Docker startup cost is ~48s ONE TIME, not 22 times.

**The combination produces a deterministic ~3-5 minute wall time** for the full integration suite on Docker Desktop / WSL2, well under any per-hook 180s timeout.

**Sequence:** Runs LAST in the gap-closure series. Depends on 02-07 (server-only guards — typechecks must pass before integration tests are credible) and 02-08 (env-assert good-path fixture — env-assert IS one of the integration suites we want to verify GREEN here).

**Out of scope:**
- Any change to individual integration test files (their `beforeAll`/`afterAll` shape is correct; the cascade was infrastructural). The `td`/`pg` symbol bindings inside each suite stay; `startTestDb()` returns the same container regardless of how many suites called it.
- The unit project (`workspace[0]`) — only the integration project (`workspace[1]`) is pinned to `singleFork`.
- Migration of any suite to an in-process Postgres (e.g., `pg-mem`) — out of scope.
- Changes to `testcontainers` version, `withReuse()` flags, or Ryuk reaper config (single-process singleton avoids needing them).

**Risk register:**
- `singleFork: true` lengthens wall-time worst-case (no parallelism). Document the new ceiling (≤ 8 minutes on a typical Windows + Docker Desktop machine) in `docs/ops/integration-tests.md`. CI machines without Docker contention often complete in ≤ 4 minutes.
- The vitest 3.0.5 workspace API supports `globalSetup` at the inline-project level. Verify the field name (`globalSetup`, not `globalsetup` or `globalSetupFiles`) by inspecting the vitest 3.0.5 type definitions. If `globalSetup` is not honored at workspace[]-entry level in 3.0.5, the fallback is to point the top-level `test.globalSetup` at the file and gate it on `process.env.VITEST_PROJECT === 'integration'`. Pick the path that compiles.
- The singleton in `tests/fixtures/db.ts` MUST NOT swallow the `start()` error on retry. If the first call fails, the singleton resolves to the rejection (cached) and EVERY subsequent suite sees the same clear error message, instead of each one re-attempting and timing out independently.
- Each test suite still mutates `process.env.*` in its own `beforeAll` (e.g., `pluggy/webhook.test.ts` sets `PLUGGY_WEBHOOK_SECRET`). With `singleFork: true`, those mutations PERSIST across suites in the same process. Do NOT reset env between suites — the existing tests are written assuming each suite's own `beforeAll` overwrites whatever the previous one set. Document this in the runbook.
- pg-boss test mode (`process.env.BOSS_TEST_MODE = '1'`) is set per-suite. Confirm by inspection that no suite RELIES on `BOSS_TEST_MODE` being unset elsewhere — grep the integration tree before flipping the flag.
- Leaked containers from prior failed runs (the debug session listed 4 still-up containers after timeout) confound any retry. The runbook must include a leak-cleanup one-liner: `docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f`.

**Acceptance proof set:**
1. **Static config check:** `grep -E "singleFork|fileParallelism|globalSetup" vitest.config.ts` returns matches.
2. **Singleton check:** `grep -nE "let _cached|return _cached" tests/fixtures/db.ts` returns ≥ 1 match.
3. **Full integration suite runs to completion:**
   ```
   docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f
   npm run test:integration
   ```
   Expected: NO `Hook timed out`, NO `Cannot read properties of undefined`. Test failures are now real assertion failures (e.g., the dependent fixes from 02-07/02-08 must be in place; an unrelated assertion failing is acceptable as long as the cascade is gone).
4. **Wall-time check:** the run completes in under 480 seconds (8 minutes) on a Windows host with Docker Desktop / WSL2 idle.
</preamble>

<objective>
1. Pin the vitest integration project to single-fork sequential execution so all 22 suites run in ONE forked worker process.
2. Register a vitest `globalSetup` (`tests/fixtures/integration-globals.ts`) that boots one Postgres testcontainer before any suite runs and stops it on teardown.
3. Refactor `tests/fixtures/db.ts` `startTestDb()` to be a module-level lazy singleton — repeated calls reuse the same container.
4. Document Docker prerequisites and leak cleanup in `docs/ops/integration-tests.md`.

Purpose: Eliminate the 22-file `afterAll` TypeError cascade (UAT Test 2) so every integration suite executes its own assertions. After this lands, the 5 currently-blocked UAT tests (3, 4, 5, 6, 7) become re-runnable.

Output: 1 vitest config update, 1 db fixture refactor, 1 new globalSetup file, 1 ops runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pluggy-ingestion/02-HUMAN-UAT.md
@.planning/debug/integration-tests-testcontainers-cascade.md
@vitest.config.ts
@tests/fixtures/db.ts
@tests/integration/security/idor.test.ts
@tests/integration/pluggy/webhook.test.ts
@package.json

<interfaces>
Current `tests/fixtures/db.ts` shape (per-call container — the bug):
```
export interface TestDb {
  url: string;
  container: StartedPostgreSqlContainer;
  stop: () => Promise<void>;
}
export async function startTestDb(): Promise<TestDb> { /* new PostgreSqlContainer().start() each call */ }
```

Target shape (singleton — the fix):
```
export interface TestDb { url: string; container: StartedPostgreSqlContainer; stop: () => Promise<void>; }

let _started: Promise<TestDb> | null = null;

export function startTestDb(): Promise<TestDb> {
  if (_started) return _started;
  _started = bootContainer();   // resolves once; rejection cached so callers see same clear error
  return _started;
}

// Internal — only called once across the process lifetime.
async function bootContainer(): Promise<TestDb> { /* exactly the current body of startTestDb */ }

// Exposed only for the globalSetup teardown — never called by individual suites.
export async function stopSharedTestDb(): Promise<void> {
  if (_started) {
    try { const td = await _started; await td.container.stop(); } catch { /* swallow on teardown */ }
    _started = null;
  }
}
```

Vitest 3.0.5 workspace integration-project shape (target):
```
{
  extends: true,
  test: {
    name: 'integration',
    include: ['tests/integration/**/*.test.{ts,tsx}'],
    environment: 'node',
    testTimeout: 60_000,
    hookTimeout: 120_000,
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
    fileParallelism: false,
    globalSetup: ['tests/fixtures/integration-globals.ts'],
  },
},
```

GlobalSetup contract (from vitest 3.0.5 docs):
```
// tests/fixtures/integration-globals.ts
export async function setup(): Promise<void> { /* called once before any test file runs */ }
export async function teardown(): Promise<void> { /* called once after every test file finishes */ }
// Or: default-export an object with { setup, teardown }; vitest accepts either shape.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor tests/fixtures/db.ts into a lazy singleton</name>
  <files>
    tests/fixtures/db.ts
  </files>
  <action>
Replace the entire `tests/fixtures/db.ts` body with the singleton-cached version. The exported `TestDb` interface is unchanged — every existing caller (`startTestDb()` returning `{ url, container, stop }`) continues to compile, but now `td.stop()` from inside an individual suite's `afterAll` becomes a no-op (the globalSetup owns the lifecycle).

```
import {
  PostgreSqlContainer,
  type StartedPostgreSqlContainer,
} from '@testcontainers/postgresql';

export interface TestDb {
  url: string;
  container: StartedPostgreSqlContainer;
  /**
   * Per-suite `afterAll` hooks call this. With the singleton + globalSetup
   * model (plan 02-09), individual suite teardown is a no-op — the shared
   * container survives until vitest globalSetup teardown runs.
   */
  stop: () => Promise<void>;
}

/**
 * Module-level cached promise. Resolves to the started TestDb the first
 * time `startTestDb()` is called within a process; every subsequent call
 * returns the same promise (so all 22 integration suites share ONE
 * Postgres container under `singleFork: true`).
 *
 * If the first start rejects, the rejection is cached too — every suite
 * sees the same clear error rather than 22 independent timeout failures.
 */
let _started: Promise<TestDb> | null = null;

/**
 * Boot a disposable Postgres 16 container for integration tests.
 *
 * Pre-requisites:
 *   - Docker Desktop with the WSL2 backend on Windows (RESEARCH.md Pitfall 9).
 *   - Network access to pull `postgres:16-alpine` on first run.
 *
 * Plan 02-09: this is now a process-level singleton. The first call within
 * a vitest process boots the container; every subsequent call returns the
 * same handle. Suite-level `afterAll(() => td.stop())` is a no-op — the
 * shared container survives until vitest globalSetup teardown.
 */
export function startTestDb(): Promise<TestDb> {
  if (_started) return _started;
  _started = bootContainer();
  return _started;
}

async function bootContainer(): Promise<TestDb> {
  let container: StartedPostgreSqlContainer;
  try {
    container = await new PostgreSqlContainer('postgres:16-alpine')
      .withDatabase('portal_test')
      .withUsername('test')
      .withPassword('test')
      .start();
  } catch (err) {
    throw new Error(
      `[startTestDb] Failed to start Postgres testcontainer. Is Docker running?\n` +
        `On Windows, Docker Desktop with the WSL2 backend is required.\n` +
        `If a previous run leaked containers, clean up with:\n` +
        `  docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f\n` +
        `Underlying error: ${(err as Error).message}`,
    );
  }

  const url = container.getConnectionUri();
  return {
    url,
    container,
    // No-op: lifecycle is owned by the globalSetup teardown.
    stop: async () => { /* shared container — see tests/fixtures/integration-globals.ts */ },
  };
}

/**
 * Vitest globalSetup teardown helper. NEVER call from a test file.
 *
 * Stops the shared container (if it was started) and clears the singleton
 * so a re-run within the same Node process (rare — vitest spawns a fresh
 * process per CLI invocation) starts cleanly.
 */
export async function stopSharedTestDb(): Promise<void> {
  if (!_started) return;
  try {
    const td = await _started;
    await td.container.stop();
  } catch {
    // Swallow on teardown — testcontainer Ryuk reaper will clean up regardless.
  } finally {
    _started = null;
  }
}
```

Why each suite's `afterAll(() => td.stop())` becomes a no-op rather than removing the call: the existing test files (21 suites) call `await td.stop()` inside their `afterAll`. Changing every test file is out of scope and risky; making `stop()` a no-op keeps the test-file shape unchanged and the lifecycle in one place (`integration-globals.ts`).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('tests/fixtures/db.ts','utf8'); const need=['let _started','stopSharedTestDb','bootContainer','singleton','postgres:16-alpine']; const missing=need.filter(k=>!f.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <done>`tests/fixtures/db.ts` exports `startTestDb()` (returns cached promise on repeat calls) and `stopSharedTestDb()`. The exported `TestDb` interface is byte-identical to before. The first call boots a Postgres 16 testcontainer; subsequent calls return the same `TestDb`.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/fixtures/integration-globals.ts vitest globalSetup</name>
  <files>
    tests/fixtures/integration-globals.ts
  </files>
  <action>
Create the globalSetup file. Vitest 3.0.5 accepts a module that exports `setup` + `teardown` named functions OR a default-exported object — use named exports for clarity.

```
/**
 * Vitest globalSetup for the integration project (plan 02-09).
 *
 * Lifecycle:
 *   1. setup()   runs ONCE before any integration test file.
 *      It triggers the singleton in tests/fixtures/db.ts so the Postgres
 *      testcontainer is ready by the time the first suite's `beforeAll`
 *      calls `startTestDb()` (which then returns the cached promise
 *      synchronously-ish — same Promise, no extra Docker call).
 *   2. teardown() runs ONCE after every integration test file completes.
 *      It stops the shared container and clears the singleton.
 *
 * Pinned together with `pool: 'forks'` + `singleFork: true` +
 * `fileParallelism: false` in `vitest.config.ts`. With these flags, the
 * module instance from step 1 is re-used across every suite (single
 * process), so the cached container survives the entire run.
 *
 * Without these flags, vitest's default file parallelism would launch
 * multiple worker processes; each would re-execute this setup() once,
 * which would defeat the point. The single-fork pin is therefore part
 * of the contract — see vitest.config.ts.
 */
import { startTestDb, stopSharedTestDb, type TestDb } from './db';

let booted: TestDb | null = null;

export async function setup(): Promise<void> {
  // Boot the shared container exactly once. Any error here surfaces as a
  // globalSetup failure with full stderr — clearer than a per-suite
  // "Hook timed out" cascade.
  booted = await startTestDb();
  // Expose the URL for any consumer that prefers reading from env over
  // calling startTestDb() directly. Existing suites continue to call
  // startTestDb() (cached singleton) so they pick up the same URL.
  process.env.TEST_DATABASE_URL = booted.url;
}

export async function teardown(): Promise<void> {
  await stopSharedTestDb();
  booted = null;
  delete process.env.TEST_DATABASE_URL;
}
```

Note: NOT a default export, NOT a class. Vitest 3.0.5 globalSetup accepts named `setup` + `teardown` functions; this is the documented contract. If, during execution, you discover that vitest 3.0.5 in this repo expects a different shape (e.g., default export of `{ setup, teardown }`), prefer a permissive form:
```
export default { setup, teardown };
export { setup, teardown };
```
This satisfies both shapes.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('tests/fixtures/integration-globals.ts','utf8'); const need=['startTestDb','stopSharedTestDb','export async function setup','export async function teardown']; const missing=need.filter(k=>!f.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <done>`tests/fixtures/integration-globals.ts` exists, imports `startTestDb` + `stopSharedTestDb` from `./db`, and exports `setup` + `teardown` async functions.</done>
</task>

<task type="auto">
  <name>Task 3: Pin vitest integration project to single-fork sequential + register globalSetup</name>
  <files>
    vitest.config.ts
  </files>
  <action>
Replace the integration project block in `vitest.config.ts` (currently lines 22-30) with the pinned version. The unit project (`workspace[0]`) stays unchanged — unit tests are CPU-bound and benefit from parallelism.

The full updated file:

```
import { defineConfig } from 'vitest/config';
import path from 'node:path';

// Vitest 3.0.5 — projects are configured via the `workspace` field.
// Renamed to `projects` only in 3.2+. We pin 3.0.5 (Wave 0 plan) so we use
// the workspace API. Same semantics: each entry is an inline project config.
export default defineConfig({
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
  test: {
    setupFiles: ['tests/setup.ts'],
    workspace: [
      {
        extends: true,
        test: {
          name: 'unit',
          include: ['tests/unit/**/*.test.{ts,tsx}'],
          environment: 'happy-dom',
        },
      },
      {
        extends: true,
        test: {
          name: 'integration',
          include: ['tests/integration/**/*.test.{ts,tsx}'],
          environment: 'node',
          testTimeout: 60_000,
          hookTimeout: 120_000,
          // ----------------------------------------------------------------
          // Plan 02-09 — gap closure (Gap 2).
          // Pin to ONE forked worker running suites SEQUENTIALLY so the
          // singleton testcontainer in tests/fixtures/db.ts survives across
          // every suite. Default Vitest parallelism (~os.cpus().length forked
          // workers, file-level parallelism) blew past the 180s hookTimeout
          // on Windows + Docker Desktop / WSL2 (per-container two-stage init
          // ballooned from ~48s to >120s under contention). The trade is
          // longer wall time (typically 3-5 minutes for the full integration
          // suite) for deterministic pass/fail. CI runs see ~3 min; local
          // Windows + Docker Desktop sees ~5-8 min.
          // ----------------------------------------------------------------
          pool: 'forks',
          poolOptions: { forks: { singleFork: true } },
          fileParallelism: false,
          // globalSetup boots the shared Postgres container once before any
          // suite runs and stops it after every suite completes. See
          // tests/fixtures/integration-globals.ts.
          globalSetup: ['tests/fixtures/integration-globals.ts'],
        },
      },
    ],
  },
});
```

If the vitest 3.0.5 type definitions reject `globalSetup` at the workspace[]-entry level (verified via `node_modules/vitest/dist/config.d.ts` — read the `WorkspaceProjectConfiguration` / `InlineConfig` type), fall back to placing `globalSetup: ['tests/fixtures/integration-globals.ts']` at the TOP-LEVEL `test` block AND adding a guard inside `integration-globals.ts`:

```
// fallback guard at the top of integration-globals.ts setup():
if (!process.env.VITEST_PROJECT_NAME?.includes('integration')) return;
```

This dual-path keeps the unit project from accidentally booting Postgres. Pick whichever path actually compiles in vitest 3.0.5 — the workspace-entry path is preferred because it scopes naturally.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('vitest.config.ts','utf8'); const need=['singleFork: true','fileParallelism: false','globalSetup','tests/fixtures/integration-globals.ts','pool: \\'forks\\'']; const missing=need.filter(k=>!f.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <done>`vitest.config.ts` integration project block has `pool: 'forks'`, `poolOptions.forks.singleFork: true`, `fileParallelism: false`, and `globalSetup: ['tests/fixtures/integration-globals.ts']`. The unit project is unchanged. `pnpm typecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 4: Run the full integration suite and document Docker prereqs</name>
  <files>
    docs/ops/integration-tests.md
  </files>
  <action>
1. Clean up any leaked containers from prior failed runs:
```
docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f
```
On Windows PowerShell, equivalent:
```
docker ps --filter "ancestor=postgres:16-alpine" -q | ForEach-Object { docker rm -f $_ }
```

2. Run the full integration suite once and capture the result. Expect ONE long-running command:
```
npm run test:integration
```
The expected outcome:
- NO `Hook timed out in 180000ms`.
- NO `TypeError: Cannot read properties of undefined (reading 'stop')`.
- NO `TypeError: Cannot read properties of undefined (reading 'end')`.
- Wall time ≤ 480s (8 minutes) on Windows + Docker Desktop / WSL2; ≤ 240s on CI / Linux.
- Real assertion failures (if any) reflect actual test logic — those are NOT a regression of this plan; they would surface from already-existing fixture/test bugs that the cascade was hiding. The goal of this plan is "tests EXECUTE", not "every test PASSES".

If the run still times out, first check:
- Docker Desktop is running and responsive (`docker info | head`).
- No other Postgres-on-5432 processes are competing for the daemon (`docker ps`).
- The leaked-container cleanup above actually removed everything.
- `tests/fixtures/integration-globals.ts` is reachable from `vitest.config.ts` (relative paths matter).

3. Create `docs/ops/integration-tests.md` documenting the contract. Aim for ≥ 40 lines:

```
# Integration Test Runbook

The integration project (`vitest --project integration`) boots ONE shared
Postgres 16 testcontainer for the entire run and executes all 22 suites
sequentially in one forked worker.

## TL;DR

```
docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f
npm run test:integration
```

## Why it is configured this way

Vitest 3.0.5's default file-parallelism (~os.cpus().length forked workers,
each running one test file concurrently) caused a 22-file `afterAll`
TypeError cascade on Windows + Docker Desktop / WSL2: per-container Postgres
init ballooned from ~48s (uncontended) to >120s under parallel Docker
pressure. The slowest suite (security/idor.test.ts) blew past its 180s
`hookTimeout`, leaving `td`/`pg` undefined; afterAll then crashed.

Plan 02-09 (gap closure) pinned the integration project to:
- `pool: 'forks'` with `singleFork: true` — one forked worker, runs suites
  sequentially.
- `fileParallelism: false` — explicit (belt and suspenders).
- `globalSetup: ['tests/fixtures/integration-globals.ts']` — boots one
  testcontainer before any suite runs, stops it after every suite finishes.

`tests/fixtures/db.ts > startTestDb()` is now a module-level lazy singleton:
the first call boots the container; every subsequent call (across all 22
suites) returns the same `TestDb`. Suite-level `afterAll(() => td.stop())`
is intentionally a no-op — the globalSetup teardown owns the lifecycle.

## Prerequisites

- Docker Desktop running with the WSL2 backend (Windows) or any healthy
  Docker daemon (macOS / Linux).
- The `postgres:16-alpine` image cached locally (first run pulls ~395 MB):
  ```
  docker pull postgres:16-alpine
  ```
- No other process competing for port 5432 — testcontainers picks an
  ephemeral host port, but a stuck Postgres container from a prior run can
  still starve the daemon.

## Cleaning up leaked containers

Testcontainers' Ryuk reaper cleans up after a clean shutdown, but a hard
SIGKILL (or a Docker daemon restart) can leak containers. Symptoms:
unexplained `cannot start service` errors; the run hangs at `setup`.

Cleanup one-liner (bash):
```
docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f
```

PowerShell equivalent:
```
docker ps --filter "ancestor=postgres:16-alpine" -q | ForEach-Object { docker rm -f $_ }
```

## Expected wall time

| Environment             | Expected | Hard ceiling |
|-------------------------|----------|--------------|
| CI (Linux + Docker)     | ~3 min   | 5 min        |
| macOS + Docker Desktop  | ~4 min   | 7 min        |
| Windows + Docker / WSL2 | ~5-7 min | 10 min       |

If a run exceeds the hard ceiling, treat it as an infrastructure regression
(not a flaky test) — investigate Docker daemon health first.

## Persistence of process.env across suites

With `singleFork: true`, all suites share one Node process. Each suite's
`beforeAll` mutates `process.env.*` (e.g., `pluggy/webhook.test.ts` sets
`PLUGGY_WEBHOOK_SECRET`; `security/idor.test.ts` sets `ENCRYPTION_KEY`).
Those mutations PERSIST across suites in run order. Existing tests are
written assuming each suite's own `beforeAll` overwrites whatever the
previous one set — do NOT add a global `process.env` reset between suites.

## Adding a new integration suite

1. Place under `tests/integration/<area>/<feature>.test.ts`.
2. Call `await startTestDb()` in `beforeAll` — the singleton returns the
   shared container; do NOT add `withReuse()` or any container-lifecycle
   hooks of your own.
3. Apply migrations once per suite (`drizzle-orm/postgres-js/migrator`) —
   migrations are idempotent against the shared schema, so this is a
   ~50ms no-op after the first suite has run.
4. Set the `process.env.*` your suite needs in `beforeAll` (overrides
   whatever the previous suite left).
5. Keep `afterAll(() => td.stop())` if your suite already has one — it is
   a no-op now but harmless. New suites can omit it entirely.

## Troubleshooting

| Symptom | Cause | Fix |
|---------|-------|-----|
| `Hook timed out in 180000ms` in globalSetup | Docker daemon unreachable or starved | Check `docker info`; clean up leaked containers |
| All 22 suites pass except env-assert | env-assert good-path fixture drift | See plan 02-08 |
| `Cannot read properties of undefined (reading 'stop')` returns | `singleFork: true` was reverted, or globalSetup file moved | Verify `vitest.config.ts`; this plan's contract is binding |
| Run takes >10 min | Docker Desktop memory pressure (Windows) | Restart Docker Desktop; bump WSL2 memory in `.wslconfig` |
```

Note: do NOT include flaky-test fixes in this runbook unless 02-09 actually surfaces them. The runbook is for the infrastructure contract; test-content fixes belong to follow-up plans if any are needed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('docs/ops/integration-tests.md','utf8'); const lines=f.split('\n').length; const need=['singleFork','globalSetup','postgres:16-alpine','tests/fixtures/db.ts','tests/fixtures/integration-globals.ts']; const missing=need.filter(k=>!f.includes(k)); if(lines<40){console.error('Too short:',lines);process.exit(1);} if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK',lines,'lines');"</automated>
  </verify>
  <done>`docs/ops/integration-tests.md` exists with ≥ 40 lines, names the singleton + globalSetup contract, documents Docker prereqs, gives a leaked-container cleanup command, and lists expected wall times. The full integration suite (`npm run test:integration`) has been run from a clean state and the cascade is gone — every suite either passes or fails on real assertions, not on `afterAll` undefined errors.</done>
</task>

</tasks>

<verification>
- Static config + fixture checks (already in per-task `<automated>`).
- **Integration suite execution** (the proof — manual confirmation):
  ```
  docker ps --filter "ancestor=postgres:16-alpine" -q | xargs -r docker rm -f
  npm run test:integration 2>&1 | tee .planning/phases/02-pluggy-ingestion/02-09-test-output.log
  ```
  Acceptance:
  - Zero occurrences of `Hook timed out`.
  - Zero occurrences of `Cannot read properties of undefined (reading 'stop')`.
  - Zero occurrences of `Cannot read properties of undefined (reading 'end')`.
  - All suites in the file list at the top of `vitest run --project integration` output have an explicit pass/fail status.
  - Wall time ≤ 480s on Windows + Docker / WSL2.
- After the suite completes, dump the leak count:
  ```
  docker ps --filter "ancestor=postgres:16-alpine" -q | wc -l
  ```
  Expected: 0 (Ryuk reaper + globalSetup teardown are both clean).
- Cross-link verification: confirm 02-08's env-assert good-path actually executes and passes inside this run (env-assert is part of `tests/integration/observability/`).
</verification>

<success_criteria>
- `vitest.config.ts` integration project pins `pool: 'forks'`, `singleFork: true`, `fileParallelism: false`, and `globalSetup`.
- `tests/fixtures/db.ts` exports `startTestDb()` (cached singleton) + `stopSharedTestDb()`.
- `tests/fixtures/integration-globals.ts` exists with `setup()` + `teardown()`.
- `npm run test:integration` runs to completion under 480s on Windows + Docker Desktop with no `Hook timed out` and no `undefined.stop()/.end()` cascade.
- Every previously-blocked UAT criterion (3, 4, 5, 6, 7) is now re-runnable: the corresponding integration suites EXECUTE.
- `docs/ops/integration-tests.md` exists and documents the contract.
- `pnpm typecheck` clean.
- No change to any individual integration test file (`tests/integration/**/*.test.ts`).
</success_criteria>

<output>
After completion, create `.planning/phases/02-pluggy-ingestion/02-09-SUMMARY.md` describing:
- Whether `globalSetup` worked at the workspace[]-entry level or required the top-level fallback.
- The actual wall-time observed for the full integration run (single number, in seconds).
- The pass/fail breakdown across all 22 suites — list any suites that failed on real assertions (those become candidates for follow-up plans, but are NOT in scope of 02-09).
- Confirmation that env-assert good-path passes (cross-check with plan 02-08).
- The leaked-container count after teardown (expected: 0).
- Any vitest 3.0.5 quirks discovered during execution (typedef gaps, default-export vs named-export shape).
- Recommended next steps if any UAT-blocked tests (3-7) still need follow-up after the cascade is gone.
</output>
