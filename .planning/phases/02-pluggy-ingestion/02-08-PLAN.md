---
phase: 02-pluggy-ingestion
plan: 08
type: execute
wave: 7
depends_on: [02-07]
gap_closure: true
files_modified:
  - tests/integration/observability/env-assert.test.ts
  - .env.example
  - docs/ops/local-dev-setup.md
autonomous: true
requirements: [OPS-04, CONN-07]
tags: [env, ops-04, fixture, gap-closure, docs]

must_haves:
  truths:
    - "tests/integration/observability/env-assert.test.ts goodProductionEnv() returns an env payload that satisfies every refine() block in src/lib/env.ts including the Phase 02 PLUGGY refine (lines 175-192)"
    - "Subprocess `tests/fixtures/env-runner/env-runner.ts` invoked with the new goodProductionEnv() exits status 0; the test 'exits 0 for a valid production env (good path)' is GREEN"
    - "The two bad-path env-assert tests (PLUGGY_ENV=sandbox override, SENTRY_ENV=staging override) still exit non-zero with stderr containing 'OPS-04 violation'"
    - ".env.example documents every Phase 02 variable explicitly and shows a one-line `cp .env.example .env.local` setup step in the leading comment block"
    - "docs/ops/local-dev-setup.md (new) documents the local-dev env-loading rules for `next dev`, `pnpm start:worker`, and `npm run test:e2e`, including which env vars are required and how to generate the secrets"
  artifacts:
    - path: "tests/integration/observability/env-assert.test.ts"
      provides: "OPS-04 boot assertion test — good path now satisfies the Phase 02 PLUGGY refine"
    - path: ".env.example"
      provides: "Authoritative env-var template with explicit `cp .env.example .env.local` instruction"
    - path: "docs/ops/local-dev-setup.md"
      provides: "Onboarding runbook explaining .env.local creation, secret generation, NODE_ENV defaults, and how `next dev` / worker / E2E tests load env"
      min_lines: 40
  key_links:
    - from: "tests/integration/observability/env-assert.test.ts goodProductionEnv()"
      to: "src/lib/env.ts third .refine() block (lines 175-192)"
      via: "fixture provides PLUGGY_CLIENT_ID, PLUGGY_CLIENT_SECRET, PLUGGY_WEBHOOK_SECRET (>=32 chars), PLUGGY_ENV='production', PLUGGY_ITEM_ID_HASH_PEPPER (>=32 chars)"
      pattern: "PLUGGY_ENV: 'production'"
    - from: ".env.example"
      to: "developer's .env.local"
      via: "one-line copy command in the leading comment block"
      pattern: "cp .env.example .env.local"
---

<preamble>
## Gap closure plan — Gap 1 (env-assert good-path fixture) + Gap 2 part (c)

**Sources:**
- `.planning/phases/02-pluggy-ingestion/02-HUMAN-UAT.md` Gap 1 missing item: "Update goodProductionEnv() ..."
- `.planning/phases/02-pluggy-ingestion/02-HUMAN-UAT.md` Gap 2 missing item: same.
- `.planning/debug/integration-tests-testcontainers-cascade.md` Root Cause 2.

**Premise:** The OPS-04 schema in `src/lib/env.ts` is correct. Commit `65c88fc` extended it with a third `.refine()` (Phase 02 PLUGGY production fields) but the integration-test fixture `goodProductionEnv()` was never updated. The fixture is the bug; the schema is not. Fixing the fixture closes the env-assert good-path failure.

This plan also refreshes `.env.example` and adds a `docs/ops/local-dev-setup.md` runbook so future contributors know exactly which env file to create and what to put in it. The `.env.local` already exists at the repo root (a developer added it during the UAT investigation) but is `.gitignore`'d; the gap report's claim that it was missing is partially stale. Documenting the contract closes the door on future drift.

**Sequence:** Runs after 02-07 (server-only guards) and BEFORE 02-09 (testcontainers infra). 02-08 is independent of 02-07 in code, but ordering it after gives a clean fail-fast checkpoint: if 02-08 verification surfaces ANY env validation issue post-02-07, it is genuinely a fixture problem, not a leak from the client bundle. 02-09 pre-supposes that env-assert can pass.

**Out of scope:**
- Any change to `src/lib/env.ts` schema, refine blocks, or `.refine()` ordering.
- Any change to `tests/fixtures/env-runner/env-runner.ts`.
- The structure/intent of the existing bad-path tests (only one new fixture field is added; bad-path tests inherit it via the spread).
- The vitest config or testcontainers infra (handled in 02-09).
- Adding or modifying real secrets in any committed file.

**Risk register:**
- Bad-path tests do `{ ...goodProductionEnv(), PLUGGY_ENV: 'sandbox' }` — adding `PLUGGY_ENV: 'production'` to the good fixture means the override `PLUGGY_ENV: 'sandbox'` still triggers OPS-04 (existing behavior).
- The env-runner subprocess inherits `process.env` (`...process.env` in the test). Real `PLUGGY_*` vars from the developer's local `.env.local` could leak into the subprocess. Mitigation: setting all PLUGGY fields explicitly in `goodProductionEnv()` overrides whatever leaks in, making the test deterministic across machines.
- Setting `SERVICE_NAME: 'web'` explicitly in the fixture (it defaults to `'web'` in the schema) makes the test deterministic against any future change of default.
- Use clearly-fake string literals (e.g., `'production-pluggy-client-id-fake-fixture'`) of the right length. The schema enforces `min(1)` on client id, `min(32)` on webhook secret + item-id pepper, and `enum(['sandbox','production'])` on env.
- `.env.example` already has the Phase 02 fields. The change is to clarify the leading comment block (point at `cp .env.example .env.local`), not to introduce variables.

**Acceptance proof set:**
1. `npm run test:integration -- env-assert` runs all 3 OPS-04 subprocess tests; all 3 pass (good path exits 0, both bad paths exit 1 with `OPS-04 violation` on stderr).
2. `.env.example` contains the literal string `cp .env.example .env.local` and a pointer to `docs/ops/local-dev-setup.md`.
3. `docs/ops/local-dev-setup.md` exists with at least 40 lines.
</preamble>

<objective>
1. Update `goodProductionEnv()` in `tests/integration/observability/env-assert.test.ts` so it satisfies all three `.refine()` blocks in `src/lib/env.ts` — specifically adding the Phase 02 PLUGGY production fields that commit `65c88fc` introduced.
2. Refresh `.env.example` so the leading comment block tells a developer exactly how to bootstrap a local `.env.local`.
3. Create `docs/ops/local-dev-setup.md` — a short, concrete runbook describing local env-loading, secret generation commands, and which file each script reads.

Purpose: Close the OPS-04 boot-assertion good-path failure (UAT Test 2 independent failure — `expected 1 to be +0`) and make the local-dev contract explicit.

Output: 1 test fixture updated, 1 example file refreshed, 1 new ops runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pluggy-ingestion/02-HUMAN-UAT.md
@.planning/debug/integration-tests-testcontainers-cascade.md
@.planning/debug/connect-env-zoderror.md
@src/lib/env.ts
@tests/integration/observability/env-assert.test.ts
@tests/fixtures/env-runner/env-runner.ts
@.env.example

<interfaces>
The Phase 02 production refine in `src/lib/env.ts` (lines 175-192):
```
.refine(
  (e) => {
    if (e.NODE_ENV !== 'production') return true;
    if (process.env.NEXT_PHASE === 'phase-production-build') return true;
    if (e.SERVICE_NAME !== 'web' && e.SERVICE_NAME !== 'worker') return true;
    return !!(
      e.PLUGGY_CLIENT_ID &&
      e.PLUGGY_CLIENT_SECRET &&
      e.PLUGGY_WEBHOOK_SECRET &&
      e.PLUGGY_ENV === 'production' &&
      e.PLUGGY_ITEM_ID_HASH_PEPPER
    );
  },
  { message: 'OPS-04 violation: PLUGGY_CLIENT_ID, PLUGGY_CLIENT_SECRET, PLUGGY_WEBHOOK_SECRET, PLUGGY_ENV=production, and PLUGGY_ITEM_ID_HASH_PEPPER are required in production for web/worker' },
);
```

Field-level constraints from the schema body:
- `PLUGGY_CLIENT_ID: z.string().min(1).optional()` — non-empty
- `PLUGGY_CLIENT_SECRET: z.string().min(1).optional()` — non-empty
- `PLUGGY_WEBHOOK_SECRET: z.string().min(32).optional()` — at least 32 chars
- `PLUGGY_ENV: z.enum(['sandbox', 'production']).optional()` — must be `'production'` to satisfy the refine
- `PLUGGY_ITEM_ID_HASH_PEPPER: z.string().min(32).optional()` — at least 32 chars
- `SERVICE_NAME: z.enum(['web', 'worker', 'migrate']).default('web')` — leave default OR set explicit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 02 PLUGGY fields to goodProductionEnv() fixture</name>
  <files>
    tests/integration/observability/env-assert.test.ts
  </files>
  <action>
1. Locate `goodProductionEnv()` (currently lines 23-39).

2. Append the five Phase 02 PLUGGY fields to the returned object plus an explicit `SERVICE_NAME: 'web'`. The new returned object must be:

```
return {
  NODE_ENV: 'production',
  DATABASE_URL: 'postgres://x:y@db.example.com:5432/prod',
  NEXTAUTH_SECRET: 'production-secret-at-least-32-chars-xxxxxxxxx',
  ENCRYPTION_KEY: Buffer.alloc(32, 1).toString('base64'),
  CPF_HASH_PEPPER: 'production-pepper-at-least-32-chars-xx',
  SENTRY_DSN: 'https://abc@oNNNN.ingest.de.sentry.io/PNNNN',
  SENTRY_ENV: 'production',
  NEXTAUTH_URL: 'https://portalfinance.app',
  AWS_ACCESS_KEY_ID: 'AKIAIOSFODNN7EXAMPLE',
  AWS_SECRET_ACCESS_KEY: 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY',
  TURNSTILE_SITE_KEY: 'prod-site-key',
  TURNSTILE_SECRET_KEY: 'prod-secret-key',
  NEXT_PUBLIC_CF_TURNSTILE_SITE_KEY: 'prod-public-key',
  // --- Phase 02 (commit 65c88fc) — required by the third .refine() block in src/lib/env.ts (lines 175-192).
  // SERVICE_NAME defaults to 'web'; we set it explicitly so a parent-env leak cannot drift the test silently.
  SERVICE_NAME: 'web',
  PLUGGY_ENV: 'production',
  PLUGGY_CLIENT_ID: 'production-pluggy-client-id-fake-fixture',
  PLUGGY_CLIENT_SECRET: 'production-pluggy-client-secret-fake-fixture-32-chars-min',
  PLUGGY_WEBHOOK_SECRET: 'production-pluggy-webhook-secret-fake-fixture-32-chars',
  PLUGGY_ITEM_ID_HASH_PEPPER: 'production-pluggy-item-id-pepper-fake-fixture-32',
};
```

3. Verify field-length constraints by inspection — every PLUGGY_WEBHOOK_SECRET / PLUGGY_ITEM_ID_HASH_PEPPER literal must be ≥ 32 chars. The values above are 53 and 48 chars respectively. Use any literals of the right length you prefer; just ensure no real secret is committed.

4. **Do NOT modify** any of the three `it(...)` blocks (lines 67-95). The two bad-path tests use the spread `{ ...goodProductionEnv(), PLUGGY_ENV: 'sandbox' }` (or `SENTRY_ENV: 'staging'`). They continue to violate OPS-04 after this fix:
   - PLUGGY_ENV=sandbox bad-path: violates the FIRST refine block (sandbox-in-prod) because `e.PLUGGY_ENV !== 'production'`.
   - SENTRY_ENV=staging bad-path: violates the FIRST refine block (`SENTRY_ENV !== 'production'`).
   - Good-path: now satisfies all three refine blocks.

5. Run the test:
```
npm run test:integration -- env-assert --reporter=verbose
```
Expected: all three tests pass.
  </action>
  <verify>
    <automated>npm run test:integration -- env-assert --reporter=dot</automated>
  </verify>
  <done>`tests/integration/observability/env-assert.test.ts > exits 0 for a valid production env (good path)` passes. The two bad-path tests still pass (status non-zero + 'OPS-04 violation' in stderr). No other test or fixture file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh .env.example with explicit local-dev bootstrap instructions</name>
  <files>
    .env.example
  </files>
  <action>
1. Read the current `.env.example`. It already contains all Phase 02 PLUGGY_* fields plus the base + Sentry + Turnstile + AWS blocks. Do NOT touch the variable lines.

2. Replace the leading comment block (currently lines 1-3) with this expanded version:

```
# Portal Finance — environment variable reference.
#
# LOCAL-DEV BOOTSTRAP:
#   1. Copy this file to .env.local (gitignored):
#        cp .env.example .env.local
#   2. Fill in the blank values. Generate secrets with:
#        NEXTAUTH_SECRET / CPF_HASH_PEPPER / PLUGGY_ITEM_ID_HASH_PEPPER / PLUGGY_WEBHOOK_SECRET
#          openssl rand -base64 48
#        ENCRYPTION_KEY
#          openssl rand -base64 32
#
# CONSUMERS (which scripts/services read which file):
#   - `pnpm dev`                     -> .env.local + .env (Next.js auto-loads in dev).
#   - `pnpm start:worker`            -> .env.local + .env (tsx --env-file-if-exists; see package.json).
#   - `npm run test:e2e`             -> .env.local then scripts/run-e2e.ts overwrites with testcontainers DB url.
#   - `npm run test:integration`     -> testcontainers Postgres; per-test process.env overrides (no .env.local read).
#   - `pnpm start:web` (production)  -> SSM SecureString via AWS Copilot Fargate task definition.
#
# NEVER commit .env.local or any production-shaped secret. .env.local is in .gitignore.
#
# See docs/ops/local-dev-setup.md for the full setup runbook.
```

3. Confirm `.gitignore` already lists `.env.local`. Run:
```
grep -E "^\.env\.local$|^\.env\*$" .gitignore
```
If `.env.local` is NOT covered, add a line `.env.local` to `.gitignore`. (Most likely already covered by an existing `.env*` glob — verify before adding.)

4. Confirm by `grep` that the variable lines are intact:
```
grep -E "^(DATABASE_URL|NEXTAUTH_SECRET|ENCRYPTION_KEY|CPF_HASH_PEPPER|PLUGGY_ENV|PLUGGY_CLIENT_ID|PLUGGY_CLIENT_SECRET|PLUGGY_WEBHOOK_SECRET|PLUGGY_ITEM_ID_HASH_PEPPER|PLUGGY_SANDBOX_CLIENT_ID|PLUGGY_SANDBOX_CLIENT_SECRET|SENTRY_DSN|TURNSTILE_SITE_KEY|TURNSTILE_SECRET_KEY|NEXT_PUBLIC_CF_TURNSTILE_SITE_KEY|AWS_REGION|SES_FROM_EMAIL)" .env.example
```
Expected: variable lines intact (one match per variable that has a default or is documented uncommented).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.env.example','utf8'); const need=['DATABASE_URL','NEXTAUTH_SECRET','ENCRYPTION_KEY','CPF_HASH_PEPPER','PLUGGY_ENV','PLUGGY_CLIENT_ID','PLUGGY_CLIENT_SECRET','PLUGGY_WEBHOOK_SECRET','PLUGGY_ITEM_ID_HASH_PEPPER','cp .env.example .env.local','docs/ops/local-dev-setup.md']; const missing=need.filter(k=>!f.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <done>`.env.example` has an expanded leading comment block that tells the developer to run `cp .env.example .env.local`, lists the openssl secret-generation commands, names every consumer script, and points at `docs/ops/local-dev-setup.md`. All previously-existing variable lines are still present.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/ops/local-dev-setup.md runbook</name>
  <files>
    docs/ops/local-dev-setup.md
  </files>
  <action>
Create `docs/ops/local-dev-setup.md` with the following content. The file does not exist today; this is the first version. Aim for ≥ 40 lines.

```
# Local Development Setup

This runbook is the canonical answer to: "I just cloned `web/`. How do I run the
dev server?"

## 1. Prerequisites

- Node.js 24.x (see `package.json` engines).
- pnpm 9.15.x.
- Docker Desktop with the WSL2 backend (Windows) — required for integration
  tests that boot a Postgres testcontainer. NOT required to run `pnpm dev`.
- A local Postgres 16 instance OR a Docker container exposing 5432 — required
  for `pnpm dev` and `pnpm start:worker`. Quick path:
  ```
  docker run --rm -d --name portal-pg -p 5432:5432 \
    -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres \
    -e POSTGRES_DB=portal_finance_dev postgres:16-alpine
  ```

## 2. Environment files

| File             | Committed? | Loaded by                                |
|------------------|------------|------------------------------------------|
| `.env.example`   | YES        | reference only — never read at runtime   |
| `.env.local`     | NO (gitignored) | `pnpm dev`, `pnpm start:worker`, `npm run test:e2e` (initial load) |
| `.env`           | NO         | optional fallback after `.env.local`     |
| Per-test env     | n/a        | `npm run test:integration` sets `process.env.*` per test; ignores `.env.local` |
| AWS SSM SecureString | n/a    | production (Copilot Fargate task def)    |

Bootstrap your local file:
```
cp .env.example .env.local
```

Fill the blanks. Generate secrets with `openssl`:
```
# 32-byte AES-256-GCM key, base64-encoded:
openssl rand -base64 32     # -> ENCRYPTION_KEY

# 32+ char HMAC peppers and Auth.js secret:
openssl rand -base64 48     # -> NEXTAUTH_SECRET, CPF_HASH_PEPPER,
                            #    PLUGGY_ITEM_ID_HASH_PEPPER, PLUGGY_WEBHOOK_SECRET
```

`PLUGGY_SANDBOX_CLIENT_ID` and `PLUGGY_SANDBOX_CLIENT_SECRET` come from the
Pluggy dashboard (https://dashboard.pluggy.ai). They are required to exercise
the `/connect` flow against the sandbox; without them, `pnpm dev` still boots
but Pluggy Connect will fail at token-issue time.

## 3. Boot the dev server

```
pnpm install
pnpm db:migrate     # apply Drizzle migrations to your local Postgres
pnpm dev            # Next.js 16 with Turbopack; reads .env.local automatically
```

In a second terminal, start the worker:
```
pnpm start:worker   # tsx with --env-file-if-exists=.env.local
```

Visit http://localhost:3000 and sign up.

## 4. What `next dev` loads

Next.js 16 auto-loads `.env.local`, then `.env.development`, then `.env`
(later files do NOT override earlier ones). NODE_ENV is set automatically to
`'development'`. Public-prefixed vars (`NEXT_PUBLIC_*`) are inlined into the
client bundle at build time; everything else stays server-only.

The env validator at `src/lib/env.ts` is `import 'server-only'`-guarded
(plan 02-07), so any client-side import attempt fails the build with a clear
error message. If you see a runtime ZodError on `/connect`, the most likely
cause is a missing variable in `.env.local` — confirm with:
```
node -e "require('dotenv').config({path:'.env.local'}); ['DATABASE_URL','NEXTAUTH_SECRET','ENCRYPTION_KEY','CPF_HASH_PEPPER','PLUGGY_ITEM_ID_HASH_PEPPER','PLUGGY_WEBHOOK_SECRET'].forEach(k=>console.log(k, !!process.env[k]));"
```

## 5. Running tests

```
npm run test:unit          # vitest unit project — no Docker required
npm run test:integration   # vitest integration project — Docker REQUIRED (testcontainers)
npm run test:e2e           # Playwright + testcontainers — Docker REQUIRED
```

Integration tests do NOT read `.env.local`; each suite assigns the env vars it
needs in its `beforeAll` (see `tests/integration/observability/env-assert.test.ts`
for the pattern). If a fixture drifts from `src/lib/env.ts`, the OPS-04
boot-assertion good-path test catches it.

## 6. Production

Production runs on AWS Copilot Fargate in `sa-east-1`. Secrets live in SSM
SecureStrings; the entrypoint script composes `DATABASE_URL` from RDS host +
Secrets Manager creds. See `docs/ops/aws-copilot-setup.md` for the full
Copilot/IAM Identity Center bootstrap.

NEVER commit a real production secret to this repository, even in a comment.
```

This runbook is intentionally compact and concrete — it is the file an LLM (or
a new contributor) will read first when their dev server fails to boot.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('docs/ops/local-dev-setup.md','utf8'); const lines=f.split('\n').length; const need=['cp .env.example .env.local','openssl rand -base64 32','pnpm dev','pnpm start:worker','testcontainers']; const missing=need.filter(k=>!f.includes(k)); if(lines<40){console.error('Too short:',lines);process.exit(1);} if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('OK',lines,'lines');"</automated>
  </verify>
  <done>`docs/ops/local-dev-setup.md` exists, has ≥ 40 lines, names `.env.local` as the bootstrap file, lists the openssl commands, names every consumer script, and notes that production reads from SSM (not from any `.env.*` file).</done>
</task>

</tasks>

<verification>
- Env-assert good-path:
  ```
  npm run test:integration -- env-assert
  ```
  All 3 tests green.
- `.env.example` self-check (already in Task 2 verify):
  ```
  grep -E "cp .env.example .env.local|docs/ops/local-dev-setup.md" .env.example
  ```
  Both lines present.
- Runbook self-check (already in Task 3 verify):
  ```
  wc -l docs/ops/local-dev-setup.md
  ```
  ≥ 40.
- Cross-check that the env-assert run does NOT depend on testcontainers (env-assert spawns `tsx env-runner.ts` as a subprocess; no Postgres needed):
  ```
  grep -n "startTestDb" tests/integration/observability/env-assert.test.ts
  ```
  Expected: no matches. (Confirms 02-08 verification can run before 02-09 lands.)
</verification>

<success_criteria>
- `tests/integration/observability/env-assert.test.ts > exits 0 for a valid production env (good path)` passes.
- `tests/integration/observability/env-assert.test.ts` continues to pass the two bad-path tests with stderr containing `OPS-04 violation`.
- `.env.example` shows the `cp .env.example .env.local` bootstrap step and points at the new runbook.
- `docs/ops/local-dev-setup.md` exists, is ≥ 40 lines, and is concrete enough that a new contributor can follow it without asking questions.
- No source-code change in `src/lib/env.ts`, `src/lib/crypto.ts`, or any handler/route.
- `pnpm typecheck` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pluggy-ingestion/02-08-SUMMARY.md` describing:
- The five PLUGGY fields added to `goodProductionEnv()` and the explicit `SERVICE_NAME: 'web'` for determinism.
- The character-count rationale for the chosen literals (≥ 32 where required).
- The leading-comment-block rewrite of `.env.example` and any `.gitignore` adjustment.
- The structure of the new `docs/ops/local-dev-setup.md` (sections, key commands).
- Confirmation that no production secret was introduced and that the env-runner subprocess no longer depends on parent-env leakage.
- Cross-link to plan 02-09 (testcontainers infra) — required to unblock the rest of the integration suite.
</output>
