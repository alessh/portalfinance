---
phase: 01.1
plan: 02
subsystem: build
tags: [tsup, env, zod, aws, ses, iam-task-role, ops-04, sec-02, d-11]
requires:
  - "Plan 01.1-00 (SSO bootstrap)"
  - "Plan 01.1-01 Task 2 (package.json build:worker + db:migrate:prod scripts)"
provides:
  - "tsup.config.ts at repo root -- bundles worker + migrate to dist/, CJS, node22, externalizes native modules"
  - "dist/jobs/worker.js + dist/db/migrate.js (sourcemapped) consumed by Plan 01.1-03 Dockerfile + Plan 01.1-04 migrate Scheduled Job"
  - "src/lib/env.ts SERVICE_NAME enum widened to ['web','worker','migrate'] -- migrate skips TURNSTILE_* refinement"
  - "src/lib/env.ts AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY are .optional() in every environment -- enables Plan 01.1-03 SES IAM task-role pivot without re-touching env.ts"
  - "6 new env.test.ts cases (3 SERVICE_NAME=migrate behaviors + 3 AWS-creds-optional behaviors)"
affects:
  - "Plan 01.1-03 Task 3 (SES IAM pivot in src/mailer.ts) -- can now construct SESClient without explicit creds"
  - "Plan 01.1-04 migrate Scheduled Job manifest -- can SERVICE_NAME=migrate without provisioning TURNSTILE_*"
tech-stack:
  added:
    - "tsup 8.3.5 (already in devDependencies; first-time use)"
  patterns:
    - "tsup CJS / node22 / external native bindings for Docker runner"
    - "Zod refine skip-by-SERVICE_NAME for cross-service env divergence"
    - "AWS SDK default credential provider chain (env -> task role) -- env vars now strictly optional"
key-files:
  created:
    - tsup.config.ts
  modified:
    - src/lib/env.ts
    - tests/unit/lib/env.test.ts
key-decisions:
  - "Externals list uses literal strings + regex (e.g., /^@aws-sdk\\//) so every SDK client is left unbundled. Native bindings (pg, pg-boss, argon2) MUST resolve from node_modules so the Docker alpine image's ABI matches at runtime."
  - "SERVICE_NAME widened from `z.string()` to `z.enum(['web','worker','migrate'])`. The enum gives a static guarantee at compile time and mirrors the three Copilot manifests in Plan 01.1-04."
  - "The AWS-creds prod refine block was REMOVED entirely (not migrate-only-skipped) because the IAM-task-role pivot applies to all services. Failure mode is fail-closed at first SDK call (CredentialsProviderError), not silent."
  - "OPS-04 Pluggy sandbox-in-prod guard kept intact and explicitly tested under SERVICE_NAME=migrate (Test 6 in env.test.ts) to ensure the migrate widening did not weaken security posture."
requirements-completed:
  - OPS-04
  - SEC-02
duration: 8 min
completed: 2026-04-24
---

# Phase 01.1 Plan 02: tsup Bundle Config + env.ts SERVICE_NAME / IAM-Task-Role Widening Summary

Wired up the tsup bundle config that compiles `src/jobs/worker.ts` and `src/db/migrate.ts` to CJS / node22 with native bindings externalized, so the Docker runner stage can drop tsx (D-11). Then widened `src/lib/env.ts` along two axes: `SERVICE_NAME` is now a tri-valued enum that lets the migrate Scheduled Job skip the TURNSTILE_* requirement, and `AWS_ACCESS_KEY_*` are now optional in every environment so Plan 01.1-03's SES IAM task-role pivot can land without re-touching this file. OPS-04 sandbox-in-prod guard remains intact and explicitly tested under SERVICE_NAME=migrate.

## Execution Stats

- Duration: 8 min
- Tasks: 2 (both auto, Task 2 was TDD-style with 6 new test cases written alongside implementation)
- Files: 1 created (tsup.config.ts), 2 modified (src/lib/env.ts, tests/unit/lib/env.test.ts)

## Tasks Completed

| Task | Files | Commit |
|------|-------|--------|
| 1. tsup config bundling worker + migrate (CJS/node22) | tsup.config.ts | a33235b |
| 2. env.ts SERVICE_NAME widening + AWS creds optional + 6 new tests | src/lib/env.ts, tests/unit/lib/env.test.ts | 248cc8d |

## Build Output

`pnpm build:worker` -> tsup v8.3.5, build success in 29 ms.

| Bundle | Size |
|--------|------|
| dist/db/migrate.js | 2.01 KB |
| dist/jobs/worker.js | 45.37 KB |
| dist/db/migrate.js.map | 1.84 KB |
| dist/jobs/worker.js.map | 94.09 KB |

The migrate bundle was smoke-loaded with `node -e "require('./dist/db/migrate.js')"` -- it threw the expected `Error: DATABASE_URL is required for migration` from `main()`, confirming CJS load + module resolution work as designed.

## Verification Results

- `pnpm build:worker` -> exit 0
- All 8 acceptance criteria for Task 1 -> PASS (tsup.config.ts exists, format cjs, target node22, both entries, externals named, bundles non-empty, CJS-loadable)
- All 9 acceptance criteria for Task 2 -> PASS (SERVICE_NAME / migrate / D-11 / SEC-02 / AWS_*_optional / PLUGGY_ENV grep, vitest run, typecheck)
- `pnpm vitest run tests/unit/lib/env.test.ts` -> 12/12 PASS in 123 ms (6 existing OPS-04 cases + 3 new SERVICE_NAME=migrate cases + 3 new AWS-creds-optional cases)
- `pnpm typecheck` -> exit 0 (no TS regressions; SERVICE_NAME enum integrates cleanly with downstream consumers)
- `pnpm test:unit` -> 10 files, 53 tests, all PASS (no regressions in env / cpf / crypto / pii-scrubber / password / logger / ConsentScreen / health)

## Deviations from Plan

**[Rule 1 - Bug] Plan referenced wrong test path** -- Found during: Task 2 read_first | Plan 01.1-02 frontmatter and `<files>` block name `src/lib/env.test.ts`, but the actual repo path is `tests/unit/lib/env.test.ts` (consistent with every other test in the project, including the env.test.ts referenced in Plan 01.1-01). | Fix: Edited the actual file at `tests/unit/lib/env.test.ts`; left the plan unchanged (planning-document edit out of scope for this plan). | Files modified: `tests/unit/lib/env.test.ts` (instead of the non-existent `src/lib/env.test.ts`) | Verification: All 12 cases pass under the unit project; no orphan test file. | Commit hash: 248cc8d

**Total deviations:** 1 auto-fixed (path correction). **Impact:** none -- functionality identical to the plan's intent.

## Issues Encountered

None.

## Self-Check: PASSED

Ready for Plan 01.1-03 (Dockerfile + .dockerignore + entrypoint shim + SES IAM task-role pivot). Plan 01.1-03 Task 3 can now modify only `src/mailer.ts` -- env.ts is already prepared for the pivot.
