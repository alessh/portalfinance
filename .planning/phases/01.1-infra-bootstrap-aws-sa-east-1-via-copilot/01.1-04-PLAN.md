---
phase: 01.1
plan: 04
type: execute
wave: 2
depends_on: ["01.1-01", "01.1-02", "01.1-03"]
files_modified:
  - copilot/.workspace
  - copilot/environments/prod/manifest.yml
  - copilot/environments/addons/rds-postgres.yml
  - copilot/web/manifest.yml
  - copilot/worker/manifest.yml
  - copilot/migrate/manifest.yml
  - scripts/validate-phase-01.1.sh
autonomous: true
requirements:
  - OPS-01
  - OPS-04
  - LGPD-05
  - SEC-02

must_haves:
  truths:
    - "`copilot svc package --name web --env prod` emits valid CloudFormation JSON (manifest shape is valid)"
    - "`copilot svc package --name worker --env prod` and `--name migrate` (via `copilot job package`) also validate"
    - "RDS addon template declares PubliclyAccessible: false (T-01.1-RDS-PUB mitigation)"
    - "RDS addon template uses Engine: postgres + EngineVersion: '16' + DBInstanceClass: db.t4g.micro (D-19)"
    - "All three service manifests declare network.vpc.placement: private (D-08, D-18)"
    - "Environment manifest enables VPC endpoints: ssm, ssmmessages, secretsmanager, ecr.api, ecr.dkr, sts, logs, s3 (D-18)"
    - "web manifest http.healthcheck.path = /api/health (D-24)"
    - "migrate job has on.schedule: 'none' (manual-only trigger, D-07)"
    - "Every secret path follows `/copilot/portalfinance/prod/secrets/<NAME>` (D-05)"
    - "worker manifest has count: 1 and NO autoscaling (D-04 pg-boss singleton preservation)"
    - "scripts/validate-phase-01.1.sh skeleton exists and is executable"
  artifacts:
    - path: "copilot/.workspace"
      provides: "Copilot app name marker"
      contains: "application: portalfinance"
    - path: "copilot/environments/prod/manifest.yml"
      provides: "Single prod environment with VPC endpoints + NAT (D-02, D-18)"
      contains: "name: prod"
    - path: "copilot/environments/addons/rds-postgres.yml"
      provides: "Environment-level CFN addon for RDS Postgres 16 in private subnets"
      contains: "AWS::RDS::DBInstance"
    - path: "copilot/web/manifest.yml"
      provides: "Load Balanced Web Service manifest with /api/health target group check"
      contains: "type: Load Balanced Web Service"
    - path: "copilot/worker/manifest.yml"
      provides: "Backend Service manifest for pg-boss consumers (count: 1)"
      contains: "type: Backend Service"
    - path: "copilot/migrate/manifest.yml"
      provides: "Scheduled Job manifest with on.schedule: none (manual trigger)"
      contains: "type: Scheduled Job"
    - path: "scripts/validate-phase-01.1.sh"
      provides: "Chained infra-gate runner consumed by Plan 01.1-07 and /gsd-verify-work"
      contains: "set -eu"
  key_links:
    - from: "copilot/web/manifest.yml http.healthcheck.path"
      to: "src/app/api/health/route.ts"
      via: "ALB target group"
      pattern: "path: '/api/health'"
    - from: "copilot/environments/addons/rds-postgres.yml DBInstance"
      to: "private subnets"
      via: "DBSubnetGroup + PubliclyAccessible: false"
      pattern: "PubliclyAccessible: false"
    - from: "copilot/web/manifest.yml secrets:"
      to: "/copilot/portalfinance/prod/secrets/<NAME>"
      via: "SSM SecureString"
      pattern: "/copilot/portalfinance/prod/secrets/"
---

<objective>
Wave 2. Write the full Copilot manifest tree + environment-level RDS addon CFN template + the validation gate skeleton. Output stays local (no AWS API calls) -- Wave 3 runs `copilot init` / `copilot deploy`.

Purpose: Declarative infrastructure -- one git-versioned source of truth for the topology (D-01..D-24). The RDS addon lives at `copilot/environments/addons/` (NOT `copilot/web/addons/`) per RESEARCH Pattern 1 recommendation option (b) so worker + migrate can consume the outputs.

Output: 7 YAML / shell files. All valid per `copilot svc package`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-RESEARCH.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-VALIDATION.md
@Dockerfile
@scripts/entrypoint.sh
@scripts/run-e2e.ts

<interfaces>
RESEARCH.md canonical YAML shapes (copy verbatim, adjust only the citations):
- Pattern 1 (lines 365-487): RDS addon CFN template (adjust to env-level by dropping workload-scoped refs)
- Pattern 2 (lines 489-549): Load Balanced Web Service manifest
- Pattern 3 (lines 551-596): Scheduled Job manifest
- Pattern 5 (lines 633-693): Dockerfile shape already in use

Env-addon adjustment: in Pattern 1, the `!ImportValue` expressions `${App}-${Env}-PrivateSubnet1/2`, `${App}-${Env}-VpcId`, `${App}-${Env}-EnvironmentSecurityGroup` are Copilot-managed env-level exports -- valid from an env-level addon context.

RDS addon outputs (Copilot convention: Outputs become SCREAMING_SNAKE_CASE env vars on all services):
  DbEndpoint -> DB_ENDPOINT
  DbPort     -> DB_PORT
  DbName     -> DB_NAME
  DbSecretArn -> DB_SECRET_ARN
  DBAccessPolicy -> attached to task IAM role automatically

VALIDATION.md per-task verification map ties each manifest key to a specific infra-gate shell command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write copilot/.workspace + environment manifest + RDS addon CFN template (D-01, D-02, D-08, D-18, D-19)</name>
  <files>copilot/.workspace, copilot/environments/prod/manifest.yml, copilot/environments/addons/rds-postgres.yml</files>
  <read_first>
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-RESEARCH.md Pattern 1 (lines 365-487) full text
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md D-01, D-02, D-08, D-14, D-17, D-18, D-19, D-22
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md section "copilot/* manifests"
  </read_first>
  <action>
Create three files.

1) `copilot/.workspace`:
```yaml
application: portalfinance
```

2) `copilot/environments/prod/manifest.yml` (single prod env per D-02, VPC endpoints per D-18, ACM cert placeholder per D-14 -- will be filled in Plan 01.1-07):
```yaml
# Copilot environment manifest -- Portal Finance prod.
#
# Plan 01.1-04 / D-02 (single prod), D-14 (ACM public cert ARN),
# D-17 (single account), D-18 (VPC endpoints + NAT),
# D-22 (project tags).
#
# The `certificates` array is populated by Plan 01.1-07 after ACM
# issues the cert. Leave the key present with a placeholder so the
# manifest parses; Copilot requires an existing ARN at `env deploy`
# time but not at `env init`.
name: prod
type: Environment

network:
  vpc:
    cidr: 10.20.0.0/16
    subnets:
      public:
        - cidr: 10.20.0.0/24
          az: sa-east-1a
        - cidr: 10.20.1.0/24
          az: sa-east-1b
      private:
        - cidr: 10.20.10.0/24
          az: sa-east-1a
        - cidr: 10.20.11.0/24
          az: sa-east-1b
  # NAT gateway stays -- external traffic to Pluggy, ASAAS, Gemini, Sentry, SES.
  # VPC endpoints keep AWS-service traffic off NAT (lower cost + private).
  vpc_endpoints:
    - ssm
    - ssmmessages
    - secretsmanager
    - ecr.api
    - ecr.dkr
    - sts
    - logs
    - s3  # gateway endpoint; free

http:
  public:
    # Filled in by Plan 01.1-07 once ACM issues the cert.
    # certificates:
    #   - arn:aws:acm:sa-east-1:<account-id>:certificate/<uuid>
    ingress:
      cdn: false  # Cloudflare sits in front, not AWS CloudFront

observability:
  container_insights: false  # cost-saver; revisit Phase 6

tags:
  Project: PortalFinance
  Phase: '01.1'
```

3) `copilot/environments/addons/rds-postgres.yml` (environment-level addon per RESEARCH Pattern 1 recommendation option b; adapted so worker + migrate can consume the outputs). COPY the canonical shape VERBATIM, then apply env-level tweaks:

```yaml
# RDS Postgres 16 environment addon for Portal Finance prod.
#
# Plan 01.1-04 / D-01 (traditional RDS, not Aurora), D-08 (private
# subnets, PubliclyAccessible false), D-19 (db.t4g.micro, 20 GB gp3,
# Multi-AZ disabled in v1).
#
# This is an ENVIRONMENT addon (RESEARCH Pattern 1 recommendation b)
# so outputs are visible to all services deployed into prod (web,
# worker, migrate). Copilot exposes Outputs as SCREAMING_SNAKE_CASE
# env vars; entrypoint.sh consumes DB_ENDPOINT / DB_PORT / DB_NAME /
# DB_SECRET_ARN to compose DATABASE_URL.
#
# Source shape: https://aws.github.io/copilot-cli/docs/developing/addons/environment/
AWSTemplateFormatVersion: '2010-09-09'
Parameters:
  App:
    Type: String
    Description: Copilot application name (portalfinance)
  Env:
    Type: String
    Description: Copilot environment name (prod)

Resources:
  DBSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupDescription: !Sub '${App}-${Env}-rds-subnet-group'
      SubnetIds:
        - Fn::ImportValue: !Sub '${App}-${Env}-PrivateSubnet1'
        - Fn::ImportValue: !Sub '${App}-${Env}-PrivateSubnet2'
      Tags:
        - Key: Project
          Value: PortalFinance
        - Key: Phase
          Value: '01.1'

  DBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: !Sub '${App}-${Env}-rds-sg'
      VpcId:
        Fn::ImportValue: !Sub '${App}-${Env}-VpcId'
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          SourceSecurityGroupId:
            Fn::ImportValue: !Sub '${App}-${Env}-EnvironmentSecurityGroup'

  DBMasterSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${App}/${Env}/rds/master'
      Description: 'RDS Postgres master credentials (auto-managed by RDS)'
      GenerateSecretString:
        SecretStringTemplate: '{"username": "portalfinance"}'
        GenerateStringKey: 'password'
        ExcludeCharacters: '"@/\'
        PasswordLength: 32

  DBInstance:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Snapshot
    UpdateReplacePolicy: Snapshot
    Properties:
      DBInstanceIdentifier: !Sub '${App}-${Env}-db'
      DBName: portalfinance
      Engine: postgres
      EngineVersion: '16'
      DBInstanceClass: db.t4g.micro
      AllocatedStorage: '20'
      StorageType: gp3
      StorageEncrypted: true
      MasterUsername: !Sub '{{resolve:secretsmanager:${DBMasterSecret}:SecretString:username}}'
      MasterUserPassword: !Sub '{{resolve:secretsmanager:${DBMasterSecret}:SecretString:password}}'
      DBSubnetGroupName: !Ref DBSubnetGroup
      VPCSecurityGroups:
        - !GetAtt DBSecurityGroup.GroupId
      PubliclyAccessible: false
      MultiAZ: false
      BackupRetentionPeriod: 7
      DeletionProtection: true
      CopyTagsToSnapshot: true
      Tags:
        - Key: Project
          Value: PortalFinance
        - Key: Phase
          Value: '01.1'

  DBSecretAttachment:
    Type: AWS::SecretsManager::SecretTargetAttachment
    Properties:
      SecretId: !Ref DBMasterSecret
      TargetId: !Ref DBInstance
      TargetType: AWS::RDS::DBInstance

  DBAccessPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: !Sub '${App}-${Env}-rds-access'
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - secretsmanager:GetSecretValue
            Resource: !Ref DBMasterSecret

Outputs:
  DbEndpoint:
    Description: RDS Postgres endpoint address
    Value: !GetAtt DBInstance.Endpoint.Address
  DbPort:
    Description: RDS Postgres endpoint port
    Value: !GetAtt DBInstance.Endpoint.Port
  DbName:
    Description: Initial database name
    Value: portalfinance
  DbSecretArn:
    Description: Secrets Manager ARN holding master credentials
    Value: !Ref DBMasterSecret
  DBAccessPolicy:
    # Outputs named after ManagedPolicy ARNs are attached by Copilot to
    # every task role in the environment automatically.
    Description: Managed policy granting GetSecretValue on DBMasterSecret
    Value: !Ref DBAccessPolicy
```

Commit:
```
deploy(copilot): scaffold prod env manifest + RDS Postgres addon

D-01 / D-02 / D-08 / D-18 / D-19 -- private RDS, VPC endpoints,
PubliclyAccessible false, Multi-AZ off, DeletionProtection on.

<plan 01.1><phase 04><task 1>
```
  </action>
  <verify>
    <automated>test -f copilot/.workspace &amp;&amp; grep -q "application: portalfinance" copilot/.workspace &amp;&amp; test -f copilot/environments/prod/manifest.yml &amp;&amp; grep -q "name: prod" copilot/environments/prod/manifest.yml &amp;&amp; grep -q "10.20.0.0/16" copilot/environments/prod/manifest.yml &amp;&amp; grep -q "secretsmanager" copilot/environments/prod/manifest.yml &amp;&amp; test -f copilot/environments/addons/rds-postgres.yml &amp;&amp; grep -q "PubliclyAccessible: false" copilot/environments/addons/rds-postgres.yml &amp;&amp; grep -q "EngineVersion: '16'" copilot/environments/addons/rds-postgres.yml &amp;&amp; grep -q "db.t4g.micro" copilot/environments/addons/rds-postgres.yml &amp;&amp; grep -q "StorageEncrypted: true" copilot/environments/addons/rds-postgres.yml &amp;&amp; grep -q "DeletionProtection: true" copilot/environments/addons/rds-postgres.yml</automated>
  </verify>
  <acceptance_criteria>
    - `copilot/.workspace` contains `application: portalfinance`
    - `copilot/environments/prod/manifest.yml` contains `name: prod`, `10.20.0.0/16`, VPC endpoints list including `ssm`, `secretsmanager`, `ecr.api`, `ecr.dkr`, `sts`, `logs`, `s3`
    - `copilot/environments/addons/rds-postgres.yml` contains: `AWS::RDS::DBInstance`, `Engine: postgres`, `EngineVersion: '16'`, `DBInstanceClass: db.t4g.micro`, `PubliclyAccessible: false`, `StorageEncrypted: true`, `DeletionProtection: true`, `BackupRetentionPeriod: 7`
    - Outputs block includes `DbEndpoint`, `DbPort`, `DbName`, `DbSecretArn`, `DBAccessPolicy`
    - YAML parses via `yq` or `python -c 'import yaml; yaml.safe_load(open("copilot/environments/addons/rds-postgres.yml"))'` exits 0
  </acceptance_criteria>
  <done>Environment manifest + RDS addon in place; YAML parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Write copilot/web/manifest.yml + copilot/worker/manifest.yml (D-03, D-04, D-05, D-19, D-22, D-23, D-24)</name>
  <files>copilot/web/manifest.yml, copilot/worker/manifest.yml</files>
  <read_first>
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-RESEARCH.md Pattern 2 (lines 489-549) full YAML
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md D-03, D-04, D-05, D-19, D-22, D-23, D-24
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md "Cross-cutting rules for every manifest YAML"
    - src/app/api/health/route.ts (from Plan 01.1-01 -- confirms /api/health is the probe path)
  </read_first>
  <action>
Create two manifest files.

1) `copilot/web/manifest.yml` (Load Balanced Web Service per D-03):
```yaml
# Copilot Load Balanced Web Service -- Portal Finance web (Next.js).
#
# Plan 01.1-04 / D-03 (public ALB), D-05 (SSM secrets), D-19 (0.5 vCPU),
# D-22 (tags), D-24 (/api/health probe).

name: web
type: Load Balanced Web Service

image:
  build:
    dockerfile: Dockerfile
    context: .
  port: 8080

http:
  path: '/'
  healthcheck:
    path: '/api/health'
    port: 8080
    success_codes: '200'
    healthy_threshold: 2
    unhealthy_threshold: 3
    interval: 30s
    timeout: 5s
    grace_period: 60s
  # Plan 01.1-07 populates alias after ACM cert issuance + Cloudflare DNS.
  alias: portalfinance.app

cpu: 512
memory: 1024
platform: linux/x86_64
count: 1
exec: true  # ECS Exec for in-VPC psql + oncall access (Claude's Discretion)

variables:
  NODE_ENV: production
  SENTRY_ENV: production
  AWS_REGION: sa-east-1
  PORT: '8080'
  LOG_LEVEL: info
  SERVICE_NAME: web
  SES_FROM_EMAIL: no-reply@portalfinance.app
  NEXTAUTH_URL: https://portalfinance.app
  TURNSTILE_SITE_KEY: '0x4AAAAAA...'           # public key -- safe in manifest
  NEXT_PUBLIC_CF_TURNSTILE_SITE_KEY: '0x4AAAAAA...'

secrets:
  NEXTAUTH_SECRET: /copilot/portalfinance/prod/secrets/NEXTAUTH_SECRET
  ENCRYPTION_KEY: /copilot/portalfinance/prod/secrets/ENCRYPTION_KEY
  CPF_HASH_PEPPER: /copilot/portalfinance/prod/secrets/CPF_HASH_PEPPER
  SENTRY_DSN: /copilot/portalfinance/prod/secrets/SENTRY_DSN
  TURNSTILE_SECRET_KEY: /copilot/portalfinance/prod/secrets/TURNSTILE_SECRET_KEY
  # NO AWS_ACCESS_KEY_* -- SES uses task role (Plan 01.1-03 Task 3 pivot).

network:
  vpc:
    placement: private  # ALB lives in public subnets; task runs in private

logging:
  retention: 30  # D-23

tags:
  Project: PortalFinance
  Phase: '01.1'
```

NOTE: `TURNSTILE_SITE_KEY` placeholder `0x4AAAAAA...` will be replaced with the real public key value during Plan 01.1-05 when secrets are materialised. The value is PUBLIC, so it's safe to commit; the placeholder marker makes the "needs substitution" step explicit.

2) `copilot/worker/manifest.yml` (Backend Service per D-04):
```yaml
# Copilot Backend Service -- Portal Finance worker (pg-boss consumers).
#
# Plan 01.1-04 / D-04 (backend, no ALB), D-05 (SSM secrets), D-19
# (0.25 vCPU, 512 MB).
#
# Fixed count: 1 -- pg-boss per-user singleton keys assume a single
# replica. Horizontal scaling deferred to Phase 6.

name: worker
type: Backend Service

image:
  build:
    dockerfile: Dockerfile
    context: .
  # No port -- no ALB, no public listener.

command: ["node", "dist/jobs/worker.js"]

cpu: 256
memory: 512
platform: linux/x86_64
count: 1
exec: true

variables:
  NODE_ENV: production
  SENTRY_ENV: production
  AWS_REGION: sa-east-1
  LOG_LEVEL: info
  SERVICE_NAME: worker
  SES_FROM_EMAIL: no-reply@portalfinance.app
  NEXTAUTH_URL: https://portalfinance.app

secrets:
  NEXTAUTH_SECRET: /copilot/portalfinance/prod/secrets/NEXTAUTH_SECRET
  ENCRYPTION_KEY: /copilot/portalfinance/prod/secrets/ENCRYPTION_KEY
  CPF_HASH_PEPPER: /copilot/portalfinance/prod/secrets/CPF_HASH_PEPPER
  SENTRY_DSN: /copilot/portalfinance/prod/secrets/SENTRY_DSN

network:
  vpc:
    placement: private

logging:
  retention: 30

tags:
  Project: PortalFinance
  Phase: '01.1'
```

Commit:
```
deploy(copilot): add web (LB) and worker (Backend) service manifests

D-03 / D-04 / D-24 -- /api/health probe, private placement,
task-role based SES, pg-boss singleton preserved (count: 1).

<plan 01.1><phase 04><task 2>
```
  </action>
  <verify>
    <automated>test -f copilot/web/manifest.yml &amp;&amp; grep -q "type: Load Balanced Web Service" copilot/web/manifest.yml &amp;&amp; grep -q "path: '/api/health'" copilot/web/manifest.yml &amp;&amp; grep -q "placement: private" copilot/web/manifest.yml &amp;&amp; grep -q "retention: 30" copilot/web/manifest.yml &amp;&amp; grep -q "SERVICE_NAME: web" copilot/web/manifest.yml &amp;&amp; test -f copilot/worker/manifest.yml &amp;&amp; grep -q "type: Backend Service" copilot/worker/manifest.yml &amp;&amp; grep -q "count: 1" copilot/worker/manifest.yml &amp;&amp; grep -q "dist/jobs/worker.js" copilot/worker/manifest.yml &amp;&amp; grep -q "SERVICE_NAME: worker" copilot/worker/manifest.yml &amp;&amp; ! grep -q "AWS_ACCESS_KEY_ID" copilot/web/manifest.yml &amp;&amp; ! grep -q "AWS_ACCESS_KEY_ID" copilot/worker/manifest.yml</automated>
  </verify>
  <acceptance_criteria>
    - `copilot/web/manifest.yml` contains `type: Load Balanced Web Service`, `path: '/api/health'`, `placement: private`, `retention: 30`, `SERVICE_NAME: web`, `port: 8080`, `cpu: 512`, `memory: 1024`
    - `copilot/worker/manifest.yml` contains `type: Backend Service`, `count: 1`, `command: ["node", "dist/jobs/worker.js"]`, `SERVICE_NAME: worker`, `cpu: 256`, `memory: 512`, `placement: private`, `retention: 30`
    - NEITHER manifest contains `AWS_ACCESS_KEY_ID` (IAM task role pivot)
    - BOTH manifests contain `Project: PortalFinance` and `Phase: '01.1'` tags
    - ALL secrets paths match regex `/copilot/portalfinance/prod/secrets/[A-Z_]+`
    - YAML parses cleanly for both files
  </acceptance_criteria>
  <done>web + worker manifests ship; private placement; no long-lived AWS keys.</done>
</task>

<task type="auto">
  <name>Task 3: Write copilot/migrate/manifest.yml (D-07, D-11) + scripts/validate-phase-01.1.sh skeleton</name>
  <files>copilot/migrate/manifest.yml, scripts/validate-phase-01.1.sh</files>
  <read_first>
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-RESEARCH.md Pattern 3 (lines 551-596) full YAML
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md D-07, D-11
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-VALIDATION.md "Per-Task Verification Map" full table
    - scripts/run-e2e.ts (doc-comment + cleanup style analog)
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md section "scripts/validate-phase-01.1.sh"
  </read_first>
  <action>
Create two files.

1) `copilot/migrate/manifest.yml` (Scheduled Job with manual-only trigger per D-07):
```yaml
# Copilot Scheduled Job -- Drizzle migration runner.
#
# Plan 01.1-04 / D-07, D-11. Manually triggered via:
#   copilot job run --name migrate --env prod
# Runs `node dist/db/migrate.js` (pre-compiled via tsup; no tsx in prod).

name: migrate
type: Scheduled Job

on:
  schedule: "none"  # CRITICAL -- disables cron; manual runs only.

image:
  build:
    dockerfile: Dockerfile
    context: .

command: ["node", "dist/db/migrate.js"]

cpu: 256
memory: 512
platform: linux/x86_64
exec: true
retries: 0  # Fail-fast; Drizzle migrations are idempotent, but safe
            # replays after partial failure require human review.

variables:
  NODE_ENV: production
  SENTRY_ENV: production
  AWS_REGION: sa-east-1
  LOG_LEVEL: info
  SERVICE_NAME: migrate
  NEXTAUTH_URL: https://portalfinance.app

secrets:
  NEXTAUTH_SECRET: /copilot/portalfinance/prod/secrets/NEXTAUTH_SECRET
  ENCRYPTION_KEY: /copilot/portalfinance/prod/secrets/ENCRYPTION_KEY
  CPF_HASH_PEPPER: /copilot/portalfinance/prod/secrets/CPF_HASH_PEPPER
  SENTRY_DSN: /copilot/portalfinance/prod/secrets/SENTRY_DSN

network:
  vpc:
    placement: private

logging:
  retention: 30

tags:
  Project: PortalFinance
  Phase: '01.1'
```

2) `scripts/validate-phase-01.1.sh` (chained infra-gate runner -- SKELETON; Plan 01.1-07 fills in the full check list. For Wave 2 this script must exist + be executable + run the LOCAL-only checks). Content:

```sh
#!/bin/sh
# validate-phase-01.1.sh -- chained infra gates for Phase 01.1.
#
# Plan 01.1-04 creates the skeleton; Plan 01.1-07 Task 3 appends the
# remote AWS checks (ALB, RDS, SSM, CloudWatch retention, Cloudflare
# SSL mode). Exit 0 on all-pass; non-zero on first failure.
#
# Local-only checks are always safe to run. Remote checks require
# `aws sso login --profile portalfinance-prod` to be active.

set -eu

PROFILE="${AWS_PROFILE:-portalfinance-prod}"
APP="portalfinance"
ENV_NAME="prod"

pass() { printf '  [PASS] %s\n' "$1"; }
fail() { printf '  [FAIL] %s\n' "$1" >&2; exit 1; }

echo "== Phase 01.1 validation =="

# --- Local file gates (safe without AWS access) ---
echo "Checking local files..."

[ -f Dockerfile ] || fail "Dockerfile missing"
[ -f .dockerignore ] || fail ".dockerignore missing"
[ -f scripts/entrypoint.sh ] || fail "scripts/entrypoint.sh missing"
[ -x scripts/entrypoint.sh ] || fail "scripts/entrypoint.sh not executable"
[ -f copilot/.workspace ] || fail "copilot/.workspace missing"
[ -f copilot/environments/prod/manifest.yml ] || fail "prod env manifest missing"
[ -f copilot/environments/addons/rds-postgres.yml ] || fail "RDS addon missing"
[ -f copilot/web/manifest.yml ] || fail "web manifest missing"
[ -f copilot/worker/manifest.yml ] || fail "worker manifest missing"
[ -f copilot/migrate/manifest.yml ] || fail "migrate manifest missing"
[ -f src/app/api/health/route.ts ] || fail "health route missing"
[ -f tsup.config.ts ] || fail "tsup config missing"
[ -f docs/ops/aws-copilot-setup.md ] || fail "AWS runbook missing"
[ ! -f docs/ops/railway-setup.md ] || fail "Railway runbook still present -- rename not applied"
pass "all local files present"

# --- Manifest invariant greps (cheap, pre-deploy) ---
grep -q "PubliclyAccessible: false" copilot/environments/addons/rds-postgres.yml \
  || fail "RDS addon MUST declare PubliclyAccessible: false"
pass "RDS PubliclyAccessible: false"

grep -q "placement: private" copilot/web/manifest.yml \
  || fail "web manifest MUST place tasks in private subnets"
grep -q "placement: private" copilot/worker/manifest.yml \
  || fail "worker manifest MUST place tasks in private subnets"
grep -q "placement: private" copilot/migrate/manifest.yml \
  || fail "migrate manifest MUST place tasks in private subnets"
pass "all services in private subnets"

grep -q 'schedule: "none"' copilot/migrate/manifest.yml \
  || fail "migrate job MUST have schedule: none (manual-only)"
pass "migrate is manual-only"

grep -q "retention: 30" copilot/web/manifest.yml \
  || fail "web manifest MUST set log retention: 30"
grep -q "retention: 30" copilot/worker/manifest.yml \
  || fail "worker manifest MUST set log retention: 30"
grep -q "retention: 30" copilot/migrate/manifest.yml \
  || fail "migrate manifest MUST set log retention: 30"
pass "CloudWatch retention 30 days declared"

# --- Remote checks (AWS CLI) -- skipped until Plan 01.1-07 populates them ---
if [ "${SKIP_REMOTE:-0}" = "1" ]; then
  echo "SKIP_REMOTE=1 -- skipping AWS-side gates"
  echo "All local gates PASSED"
  exit 0
fi

# Plan 01.1-07 Task 3 APPENDS the following gates below this line:
#   - RDS publicly_accessible=false
#   - SSM secrets all SecureString
#   - CloudWatch log groups retention=30
#   - Cloudflare SSL=strict via API
#   - https://portalfinance.app/api/health returns 200

echo "Remote AWS gates not yet implemented -- see Plan 01.1-07."
echo "All local gates PASSED."
```

`chmod +x scripts/validate-phase-01.1.sh`.

Commit:
```
deploy(copilot): add migrate Scheduled Job + phase 01.1 gate skeleton

D-07 / D-11 -- manual-trigger migrate via copilot job run;
validate-phase-01.1.sh asserts local invariants pre-deploy.

<plan 01.1><phase 04><task 3>
```
  </action>
  <verify>
    <automated>test -f copilot/migrate/manifest.yml &amp;&amp; grep -q "type: Scheduled Job" copilot/migrate/manifest.yml &amp;&amp; grep -q 'schedule: "none"' copilot/migrate/manifest.yml &amp;&amp; grep -q "dist/db/migrate.js" copilot/migrate/manifest.yml &amp;&amp; grep -q "SERVICE_NAME: migrate" copilot/migrate/manifest.yml &amp;&amp; test -x scripts/validate-phase-01.1.sh &amp;&amp; grep -q "^#!/bin/sh" scripts/validate-phase-01.1.sh &amp;&amp; grep -q "set -eu" scripts/validate-phase-01.1.sh &amp;&amp; SKIP_REMOTE=1 sh scripts/validate-phase-01.1.sh</automated>
  </verify>
  <acceptance_criteria>
    - `copilot/migrate/manifest.yml` contains `type: Scheduled Job`, `schedule: "none"`, `command: ["node", "dist/db/migrate.js"]`, `SERVICE_NAME: migrate`, `placement: private`, `retention: 30`, `retries: 0`
    - `scripts/validate-phase-01.1.sh` is executable
    - Script starts with `#!/bin/sh` + `set -eu`
    - `SKIP_REMOTE=1 sh scripts/validate-phase-01.1.sh` exits 0 against the current repo state
    - Script contains grep assertions for PubliclyAccessible: false, placement: private (x3), schedule: "none", retention: 30
  </acceptance_criteria>
  <done>migrate manifest done; validation script skeleton in place and passing locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest YAML -> CloudFormation | Any manifest error propagates to a failed `copilot env deploy`; validation catches it pre-deploy |
| Copilot secret paths -> SSM Parameter Store | Every secret key defines the `/copilot/portalfinance/prod/secrets/<NAME>` contract; later plan creates the SecureStrings |
| environment addon outputs -> task env vars | Misnamed outputs silently break entrypoint.sh -- grep gate catches common misnames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-RDS-PUB | Information Disclosure | RDS addon CFN | mitigate | CFN declares `PubliclyAccessible: false`; validate-phase gate greps the literal; security group ingress sources ONLY from Copilot-managed environment SG |
| T-01.1-SSM | Information Disclosure + Tampering | SSM secret paths | mitigate | Every `secrets:` entry in every manifest is a `/copilot/portalfinance/prod/secrets/<NAME>` path; Plan 01.1-05 enforces `--type SecureString`; acceptance greps check no `AWS_ACCESS_KEY_ID` path present (task role only) |
| T-01.1-ENV | Elevation of Privilege | SERVICE_NAME consistency | mitigate | Each manifest sets SERVICE_NAME unique (web/worker/migrate); env.ts (Plan 01.1-02) branches only on these exact literals |
| T-MANIFEST-DRIFT | Tampering | manifest YAML lifecycle | mitigate | validate-phase-01.1.sh asserts the 5 critical invariants every run (retention, placement, schedule, publicly_accessible, image path); committed to repo |
</threat_model>

<verification>
- `SKIP_REMOTE=1 sh scripts/validate-phase-01.1.sh` exits 0
- All YAML files parse (via `python -c 'import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]' copilot/**/*.yml` or equivalent)
- No manifest contains `AWS_ACCESS_KEY_ID` (IAM task-role enforcement)
- Every secret path starts with `/copilot/portalfinance/prod/secrets/`
</verification>

<success_criteria>
- [ ] copilot/.workspace, prod env manifest, RDS addon CFN all present and valid
- [ ] web + worker + migrate manifests present with all D-XX constraints encoded
- [ ] validate-phase-01.1.sh skeleton exits 0 against local state
- [ ] No `AWS_ACCESS_KEY_*` in any manifest (IAM task role pivot complete)
- [ ] All tasks in private subnets
- [ ] migrate has schedule: "none"
- [ ] CloudWatch retention: 30 declared on all three manifests
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-04-SUMMARY.md` documenting: all YAML file paths, key invariants grepped, any YAML-parse warnings, and the `SKIP_REMOTE=1` validate-phase output.
</output>
