---
phase: 01.1
plan: 01
subsystem: web
tags: [api, health, package-json, node-engine, standalone, ops]
requires:
  - "Plan 01.1-00 (SSO bootstrap)"
provides:
  - "GET /api/health -- static 200 JSON probe (DB-free), consumed by ALB target group + ECS container health check"
  - "package.json engines.node = >=22.0.0 <23.0.0 (matches D-12 Node 22 alpine runtime)"
  - "package.json start:web = node .next/standalone/server.js (closes Phase 1 deferred item)"
  - "package.json build:worker + db:migrate:prod script slots for Plans 01.1-02 and 01.1-03"
  - "next.config.ts D-10 doc-comment above the existing output: 'standalone' line"
affects:
  - "Plan 01.1-03 Dockerfile CMD targets the same standalone server"
  - "Plan 01.1-04 web manifest health check path = /api/health"
tech-stack:
  added: []
  patterns:
    - "Static, DB-free health endpoint using NextResponse.json"
    - "Production CMD = node .next/standalone/server.js (Next 16 standalone output)"
key-files:
  created:
    - src/app/api/health/route.ts
    - tests/unit/api/health.test.ts
  modified:
    - package.json
    - next.config.ts
key-decisions:
  - "/api/health stays static (no DB touch) so the probe survives RDS restarts and the entrypoint shim's pre-DATABASE_URL boot window (D-24)."
  - "Test 2 of health.test.ts is an import-graph guard implemented as a string match on the source file — avoids spawning a subprocess just to check the import graph."
  - "next.config.ts already had output: 'standalone' from Phase 1; this plan only adds the D-10 doc-comment so the rationale survives in version control."
requirements-completed:
  - OPS-01
  - OPS-04
duration: 6 min
completed: 2026-04-24
---

# Phase 01.1 Plan 01: Health Probe + Standalone Production Start Summary

Shipped the `/api/health` static-JSON probe that the ALB and ECS health checks will hit, bumped the Node engine to 22 to match the D-12 runtime image, and switched `start:web` to invoke the Next 16 standalone server binary directly (closing the Phase 1 deferred production-start item). Annotated `next.config.ts` so the D-10 rationale is co-located with the `output: 'standalone'` flag.

## Execution Stats

- Duration: 6 min
- Tasks: 3 (all auto)
- Files: 2 created, 2 modified

## Tasks Completed

| Task | Files | Commit |
|------|-------|--------|
| 1. /api/health route + unit test (TDD-style; route + tests written together because plan provided exact contents) | src/app/api/health/route.ts, tests/unit/api/health.test.ts | 203ddfc |
| 2. package.json engines.node 22, start:web standalone, build:worker + db:migrate:prod slots | package.json | 2cb9a0d |
| 3. next.config.ts D-10 doc-comment | next.config.ts | 164a9cd |

## Verification Results

- `pnpm vitest run tests/unit/api/health.test.ts` -> 2/2 PASS (10ms)
- `node -e` JSON validity + invariant check (engines, start:web, build:worker, db:migrate:prod) -> exit 0
- `pnpm install --frozen-lockfile=false` -> Done in 2.1s, no warnings beyond unsupported engine info (host node is 24.x; pnpm warns but proceeds)
- `pnpm typecheck` -> exit 0 (no TS errors introduced)
- `pnpm test:unit` -> 10 files, 47 tests, all PASS (no regressions in env / cpf / crypto / pii-scrubber / password / logger / ConsentScreen)
- All Task 1 acceptance criteria PASS (route file, runtime nodejs export, GET export, no @/db import, D-24 trace, test file)
- All Task 2 acceptance criteria PASS (engines bumped, start:web standalone, build:worker, db:migrate:prod, JSON valid, pnpm install ok)
- All Task 3 acceptance criteria PASS (D-10 comment present, output: 'standalone' preserved, typecheck green, diff is comment-only)

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0. **Impact:** none.

## Issues Encountered

None.

## Self-Check: PASSED

Ready for Plan 01.1-02 (tsup worker bundle config + env.ts widening), which can run in parallel with this plan in principle but executes sequentially in interactive mode.
