---
phase: 01.1
plan: 08
type: execute
wave: 4
depends_on: ["01.1-05", "01.1-06", "01.1-07"]
files_modified:
  - docs/ops/aws-copilot-setup.md
  - docs/ops/railway-setup.md
  - docs/ops/encryption-key-rotation.md
  - docs/ops/ses-production-access.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OPS-01
  - LGPD-05

must_haves:
  truths:
    - "`docs/ops/railway-setup.md` NO LONGER EXISTS (removed after content migrated)"
    - "`docs/ops/aws-copilot-setup.md` sections 1-7 are populated with the AWS-native equivalents of the Railway runbook"
    - "`docs/ops/encryption-key-rotation.md` references `aws ssm put-parameter --overwrite` + `copilot svc deploy`, NOT Railway dashboard"
    - "`docs/ops/ses-production-access.md` references AWS RDS (not Railway) + Copilot manifest variables (not Railway env vars)"
    - "STATE.md Deferred Items: the two Railway-related rows are closed (status -> Closed by 01.1)"
    - "`grep -r Railway docs/ops/` returns NO unintentional references (only historical notes with explicit `(superseded by 01.1)` annotation if any remain)"
  artifacts:
    - path: "docs/ops/aws-copilot-setup.md"
      provides: "Full AWS Copilot ops runbook (sections 0-7) replacing railway-setup.md"
      min_lines: 200
    - path: "docs/ops/encryption-key-rotation.md"
      provides: "ENCRYPTION_KEY / CPF_HASH_PEPPER rotation via SSM + copilot svc deploy"
      contains: "aws ssm put-parameter"
    - path: "docs/ops/ses-production-access.md"
      provides: "SES access runbook updated for Copilot manifest + task role (no Railway references)"
      contains: "Copilot manifest"
    - path: ".planning/STATE.md"
      provides: "Deferred items updated to reflect 01.1 closure"
      contains: "Closed by 01.1"
  key_links:
    - from: "docs/ops/aws-copilot-setup.md"
      to: "copilot env / svc deploy commands"
      via: "runbook step-by-step"
      pattern: "copilot (env|svc|job) (init|deploy|run)"
    - from: "docs/ops/encryption-key-rotation.md"
      to: "aws ssm put-parameter --overwrite"
      via: "rotation procedure"
      pattern: "aws ssm put-parameter --overwrite"
---

<objective>
Wave 4 (docs). Finalise the documentation migration from Railway -> AWS Copilot.

Purpose: Close the three STATE.md deferred items (Railway provisioning, standalone CMD, SES/Sentry console setup partially) by writing the replacement runbook and updating all cross-references. Delete `docs/ops/railway-setup.md` so no future reader mistakes it for current guidance.

Output: Four doc files touched, STATE.md updated. Git status clean with `docs(ops)` commits only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md
@.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md
@docs/ops/railway-setup.md
@docs/ops/encryption-key-rotation.md
@docs/ops/ses-production-access.md
@docs/ops/aws-copilot-setup.md

<interfaces>
PATTERNS.md section "docs/ops/aws-copilot-setup.md" provides the explicit section-by-section rewrite table.
Key content to carry over VERBATIM from railway-setup.md:
  - The 14-table `\dt public.*` expected output (lines 105-119)
  - "NO `accounts` table" warning (line 121) -- Phase 2 constraint
  - "drizzle-kit push is BANNED" policy (line 77)

PATTERNS.md section "docs/ops/encryption-key-rotation.md" lists 5 specific line-range edits (lines 35, 44-52, 116, 147-152, 185, 191).

PATTERNS.md section "docs/ops/ses-production-access.md" lists 3 specific line-range edits (lines 48, 113, 119, 126-127).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate docs/ops/aws-copilot-setup.md sections 1-7 by adapting docs/ops/railway-setup.md</name>
  <files>docs/ops/aws-copilot-setup.md, docs/ops/railway-setup.md</files>
  <read_first>
    - docs/ops/railway-setup.md (full file -- source of the section structure and tone)
    - docs/ops/aws-copilot-setup.md (current state -- has section 0 from Plan 01.1-00; sections 1-7 are placeholders)
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md section "docs/ops/aws-copilot-setup.md" (explicit rewrite table)
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-05-DEPLOY-LOG.md (real ARNs, DNS names, CERT_ARN to interpolate if appropriate -- redact secrets)
  </read_first>
  <action>
Read `docs/ops/railway-setup.md` and the PATTERNS.md rewrite table. For each of the 7 sections below, REPLACE the placeholder comment in `docs/ops/aws-copilot-setup.md` with the AWS-native content derived from the Railway equivalent.

### Section 1. Hard Constraint: Brazilian Territory (LGPD-05)
Keep the bold callout `**ALL personal and financial data MUST live in the Brazilian territory.**`. Replace "Railway dashboard region selector" with:
- `aws configure set region sa-east-1 --profile portalfinance-prod`
- `copilot env init --region sa-east-1 --default-config` (records region in env manifest)
- Verification: `aws rds describe-db-instances --region sa-east-1 --db-instance-identifier portalfinance-prod-db --query 'DBInstances[0].AvailabilityZone'` returns `sa-east-1a` (or 1b/1c)

### Section 2. Service Topology
Reuse Railway's 2-column table; rename columns to `Service | Copilot type | Start command`:
- web | Load Balanced Web Service | `node server.js` (container entrypoint chains via /entrypoint.sh)
- worker | Backend Service | `node dist/jobs/worker.js`
- migrate | Scheduled Job (schedule=none) | `node dist/db/migrate.js`
- RDS | Environment addon | managed

Subsection 2.1 Provisioning Steps: `copilot app init`, `copilot env init`, `copilot env deploy`, `copilot svc init` per service, `copilot svc deploy`, `copilot job run` for migrate. Include real one-liners from Plan 01.1-05 Tasks 1-3.

### Section 3. Environment Variables
Port Railway's table; change the right column from "Railway dashboard -> Service -> Variables" to `aws ssm put-parameter --type SecureString --name /copilot/portalfinance/prod/secrets/<NAME>`. List all 5 secrets (NEXTAUTH_SECRET, ENCRYPTION_KEY, CPF_HASH_PEPPER, SENTRY_DSN, TURNSTILE_SECRET_KEY).

Subsection 3.1 Verification: keep `psql -c "SELECT 1"` -- adapt to `copilot svc exec --name web -- sh -c 'apk add --no-cache postgresql-client && psql "$DATABASE_URL" -c "SELECT 1"'`.

### Section 4. Migration Workflow
Carry over the "drizzle-kit push is BANNED" paragraph VERBATIM (from Railway runbook line 77). Replace Railway's "Pre-Deploy Command" section with:
```sh
copilot job deploy --name migrate --env prod
copilot job run --name migrate --env prod
copilot job logs --name migrate --env prod --follow
```
Include the VERBATIM `\dt public.*` expected 14-row output (from railway-setup.md lines 105-119). Preserve the "NO `accounts` table" warning (line 121) -- Phase 2 guard.

### Section 5. Rollback / Halt Procedures
Replace "Railway support" with AWS Health Dashboard + `aws health describe-events`. Rollback procedure:
```sh
# List prior deploys.
copilot svc status --name web --env prod
# Redeploy the prior git-SHA image tag (ECR lifecycle keeps last 20 per D-21).
copilot svc deploy --name web --env prod --tag <prior-git-sha>
```
Schema rollback: NEVER manual SQL in prod -- write a forward-compensating Drizzle migration; commit + rerun the migrate job.

Halt procedure: `aws ecs update-service --cluster portalfinance-prod --service portalfinance-prod-web --desired-count 0` (stops serving 503) + `copilot svc status` to confirm.

### Section 6. Worker / Web Operational Notes
Carry over:
- "worker has no ALB and no public DNS" (Phase 1 invariant preserved by D-04)
- "pg-boss schema bootstrap happens on first worker boot; tolerates multiple workers attempting bootstrap -- but count stays at 1 per D-04"
- Sentry DSN must end with `de.sentry.io` (OPS-01, LGPD-05)
- Log shipping: CloudWatch only; no external aggregator in v1 (D-23)

### Section 7. Documentation of Manual Steps
Capture:
- `copilot app show --name portalfinance` output (summary of app state)
- `copilot svc show --name web --json` (ALB DNS, ARNs)
- ECS cluster name + service ARNs (for on-call runbook)
- CERT_ARN from Plan 01.1-07 Task 1
- Cloudflare zone id + API-token scope (Zone.DNS:Edit + Zone.Zone Settings:Edit only)
- Budget alarm ARN (if configured) -- note AWS Budgets 80% of $150/mo alert from D-22; document "configure per docs if not yet done"

### At the end, DELETE `docs/ops/railway-setup.md`.

```sh
git rm docs/ops/railway-setup.md
```

Commit:
```
docs(ops): replace railway-setup.md with aws-copilot-setup.md

Closes STATE.md deferred "Railway sa-east-1 provisioning" and
"standalone start command". All Railway references removed from
docs/ops/.

<plan 01.1><phase 08><task 1>
```
  </action>
  <verify>
    <automated>test -f docs/ops/aws-copilot-setup.md &amp;&amp; ! test -f docs/ops/railway-setup.md &amp;&amp; grep -q "^## 1\\." docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "^## 7\\." docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "drizzle-kit push is BANNED" docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "NO .accounts. table" docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "LGPD-05" docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "copilot job run --name migrate" docs/ops/aws-copilot-setup.md &amp;&amp; grep -q "sa-east-1" docs/ops/aws-copilot-setup.md &amp;&amp; ! grep -qiw "Railway" docs/ops/aws-copilot-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ops/aws-copilot-setup.md` contains all 8 sections (`## 0` through `## 7`)
    - File contains literal strings: `drizzle-kit push is BANNED`, `sa-east-1`, `LGPD-05`, `copilot job run --name migrate`, `Load Balanced Web Service`, `Backend Service`, `Scheduled Job`
    - `docs/ops/railway-setup.md` DOES NOT EXIST (git rm applied)
    - `docs/ops/aws-copilot-setup.md` does NOT contain the word `Railway` (case-insensitive) outside of historical annotations (if any, must be parenthesised as `(superseded by 01.1)`)
    - Expected 14-table `\dt public.*` output preserved verbatim
    - `wc -l docs/ops/aws-copilot-setup.md` >= 200
  </acceptance_criteria>
  <done>aws-copilot-setup.md replaces railway-setup.md completely; all Phase 1 policies preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub Railway references from docs/ops/encryption-key-rotation.md + ses-production-access.md</name>
  <files>docs/ops/encryption-key-rotation.md, docs/ops/ses-production-access.md</files>
  <read_first>
    - docs/ops/encryption-key-rotation.md (current state -- 5 specific line-range edits per PATTERNS.md)
    - docs/ops/ses-production-access.md (current state -- 3 specific line-range edits per PATTERNS.md)
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-PATTERNS.md sections for both files (explicit find/replace tables)
  </read_first>
  <action>
**For `docs/ops/encryption-key-rotation.md`**, apply the 5 targeted replacements from PATTERNS.md:

1. "Railway secret management policy change." -> "AWS SSM Parameter Store policy change."
2. "Set both keys in Railway" block -> two `aws ssm put-parameter --overwrite` commands for `ENCRYPTION_KEY` and `ENCRYPTION_KEY_OLD` with `--type SecureString`, followed by `copilot svc deploy --name web --env prod && copilot svc deploy --name worker --env prod`.
3. "Remove `ENCRYPTION_KEY_OLD` from Railway. Redeploy." -> `aws ssm delete-parameter --name /copilot/portalfinance/prod/secrets/ENCRYPTION_KEY_OLD` + both svc deploys.
4. CPF_HASH_PEPPER rotation block -- analogous replacements.
5. `MAINTENANCE_MODE=true in Railway` -> `aws ssm put-parameter --overwrite --type SecureString --name /copilot/portalfinance/prod/secrets/MAINTENANCE_MODE --value true` + both svc deploys.

PRESERVE the rest of the file (rotation principles, rotation-log template, heading structure). US-English only.

**For `docs/ops/ses-production-access.md`**, apply the 3 targeted replacements:

1. "Data is stored in Brazil (Railway sa-east-1)." -> "Data is stored in Brazil (AWS RDS Postgres in sa-east-1)."
2. "Set the SES_CONFIGURATION_SET=portal-finance environment variable in Railway ..." -> "Store `SES_CONFIGURATION_SET=portal-finance` under `variables:` in `copilot/web/manifest.yml` and `copilot/worker/manifest.yml` (non-secret, safe to commit)."
3. "## Step 6 -- Set environment variables in Railway" -> "## Step 6 -- Set environment variables (Copilot)" + rewrite the table column from "Railway" to "SSM path or manifest key". The `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` rows -- DELETE both and replace with a note:
   > "Production SES access uses the Copilot task-role (`DBAccessPolicy` style -- an IAM managed policy granting `ses:SendEmail` attached to the task role). NO `AWS_ACCESS_KEY_ID` required. See Plan 01.1-03 Task 3 for the SDK fallback logic."

IF the task-role policy for `ses:SendEmail` has not yet been attached (check deploy log), add a subsection at the end of Step 6 titled "### 6.1. Grant ses:SendEmail to task role" with the Copilot addon YAML pattern or `aws iam attach-role-policy` one-liner. **NOTE:** this may be a deferred follow-up; if so, cite Plan 01.1-03 Task 3 outcome + document as a TODO with a STATE.md entry.

Commit:
```
docs(ops): scrub Railway refs from key-rotation + ses-access runbooks

Rotation now uses aws ssm put-parameter --overwrite + copilot svc
deploy. SES access via IAM task role (no long-lived keys).

<plan 01.1><phase 08><task 2>
```
  </action>
  <verify>
    <automated>! grep -qiw "Railway" docs/ops/encryption-key-rotation.md &amp;&amp; grep -q "aws ssm put-parameter --overwrite" docs/ops/encryption-key-rotation.md &amp;&amp; grep -q "copilot svc deploy" docs/ops/encryption-key-rotation.md &amp;&amp; ! grep -qiw "Railway" docs/ops/ses-production-access.md &amp;&amp; grep -q "Copilot manifest" docs/ops/ses-production-access.md &amp;&amp; grep -q "AWS RDS" docs/ops/ses-production-access.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ops/encryption-key-rotation.md` does NOT contain the word `Railway` (case-insensitive)
    - `docs/ops/encryption-key-rotation.md` contains `aws ssm put-parameter --overwrite` at least twice (once for ENCRYPTION_KEY rotation, once for CPF_HASH_PEPPER)
    - `docs/ops/encryption-key-rotation.md` contains `copilot svc deploy` (post-rotation redeploy step)
    - `docs/ops/ses-production-access.md` does NOT contain the word `Railway` (case-insensitive)
    - `docs/ops/ses-production-access.md` contains `Copilot manifest` (variable placement guidance)
    - `docs/ops/ses-production-access.md` contains `AWS RDS` (LGPD-05 data-residency claim updated)
    - Neither file shrinks by more than ~10% in line count (structural content preserved)
  </acceptance_criteria>
  <done>Both ops runbooks free of Railway references; SSM + Copilot + task-role patterns in their place.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md Deferred Items table to mark Railway + standalone CMD items closed</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current Deferred Items section lines 107-113)
    - .planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-CONTEXT.md § "Phase 1 deferred items this phase closes"
  </read_first>
  <action>
Edit `.planning/STATE.md`. In the `## Deferred Items` section, change the two applicable rows:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Deploy   | Switch production start command to `node .next/standalone/server.js` (next 16 standalone output) | **Closed by 01.1-01** (package.json start:web + Dockerfile CMD) | 01-00 |
| Deploy   | Railway sa-east-1 provisioning + live schema push (Task 3 of 01-01) -- runbook ready at `docs/ops/railway-setup.md` | **Closed by 01.1-05/06** (AWS RDS Postgres 16 in sa-east-1 private, schema migrated via Copilot job; railway-setup.md removed in 01.1-08) | 01-01 |

Leave the SES/Sentry row unchanged (SES production-access 24-48h approval is a manual AWS flow; Plan 01.1-03 Task 3 + 01.1-08 Task 2 land the task-role wiring but SES "production access" (moving out of sandbox) is a separate AWS Support request and remains open as an ops action).

Also append a new subsection at the bottom of STATE.md (above the final `*Last updated*` line) documenting the phase completion:

```
## Phase 01.1 Outcome

Phase 01.1 (Infra Bootstrap -- AWS sa-east-1 via Copilot) completed.

- Copilot app `portalfinance` provisioned with prod env in sa-east-1
- RDS Postgres 16 (db.t4g.micro, private, encrypted, deletion-protected)
- 5 SSM SecureString secrets under /copilot/portalfinance/prod/secrets/
- web + worker ECS services running; migrate Scheduled Job verified idempotent
- Cloudflare orange-cloud in front of ALB; SSL mode = strict
- ACM wildcard cert issued and pinned in env manifest
- /api/health returns 200 via https://portalfinance.app/api/health
- CloudWatch retention = 30 days on all log groups
- docs/ops/railway-setup.md removed; aws-copilot-setup.md is the canonical runbook
- scripts/validate-phase-01.1.sh passes both local and remote gates

Next phase: Phase 2 (Pluggy Ingestion) can now target production sa-east-1.
```

Update the `last_updated` frontmatter timestamp to today.

Commit:
```
docs(state): close Railway deferred items; mark phase 01.1 outcome

<plan 01.1><phase 08><task 3>
```
  </action>
  <verify>
    <automated>grep -q "Closed by 01.1" .planning/STATE.md &amp;&amp; grep -q "Phase 01.1 Outcome" .planning/STATE.md &amp;&amp; grep -q "node .next/standalone/server.js" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `Closed by 01.1-01` (standalone CMD row updated)
    - `.planning/STATE.md` contains `Closed by 01.1-05/06` (Railway provisioning row updated)
    - `.planning/STATE.md` contains a new `## Phase 01.1 Outcome` section with the summary bullet list
    - `last_updated` frontmatter field reflects today's date
  </acceptance_criteria>
  <done>STATE.md reflects closed deferred items and the new phase outcome block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops runbook -> on-call engineer | Misleading instructions -> wrong prod action; scrubbing Railway refs prevents "look for the Railway dashboard" dead-ends |
| encryption-key-rotation.md -> rotation worker / engineer | Incorrect rotation command could leak keys or create a split-brain rotation state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DOC-DRIFT | Tampering | ops runbooks vs reality | mitigate | Task 1 `! grep Railway docs/ops/aws-copilot-setup.md` gate + Task 2 equivalent greps catch unscrubbed residue |
| T-KEY-ROT-ERR | Information Disclosure | encryption-key-rotation.md | mitigate | Task 2 acceptance grep ensures `aws ssm put-parameter --overwrite` AND `copilot svc deploy` are BOTH present (incomplete rotation procedure would fail one of the two greps) |
| T-OPS-REGRESS | Denial of Service | deletion of railway-setup.md | mitigate | Content is migrated to aws-copilot-setup.md with the `\dt public.*` table preserved verbatim; Task 1 acceptance grep confirms the carry-over |
</threat_model>

<verification>
- `docs/ops/railway-setup.md` removed
- `docs/ops/aws-copilot-setup.md` sections 0-7 populated, >= 200 lines
- Neither `encryption-key-rotation.md` nor `ses-production-access.md` contain `Railway`
- STATE.md Deferred Items table updated; Phase 01.1 Outcome block appended
- Full `bash scripts/validate-phase-01.1.sh` (with SSO + CF token) exits 0 -- this is the /gsd-verify-work entry point
</verification>

<success_criteria>
- [ ] aws-copilot-setup.md is the single canonical runbook (sections 0-7)
- [ ] railway-setup.md removed
- [ ] encryption-key-rotation.md scrubbed to SSM + copilot svc deploy
- [ ] ses-production-access.md scrubbed to Copilot manifest + task role
- [ ] STATE.md deferred items marked Closed by 01.1
- [ ] STATE.md Phase 01.1 Outcome block present
- [ ] `sh scripts/validate-phase-01.1.sh` (full, remote) exits 0
</success_criteria>

<output>
Create `.planning/phases/01.1-infra-bootstrap-aws-sa-east-1-via-copilot/01.1-08-SUMMARY.md` capturing: final line counts per touched doc, `! grep Railway` confirmations, STATE.md diff shape, and final validate-phase run output.
</output>
