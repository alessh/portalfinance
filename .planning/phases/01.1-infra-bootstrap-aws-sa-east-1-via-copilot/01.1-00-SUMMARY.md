---
phase: 01.1
plan: 00
subsystem: ops
tags: [infra, aws, copilot, sso, iam-identity-center, prereq]
requires: []
provides:
  - "Copilot CLI v1.34.1+ installed on developer laptop"
  - "AWS profile `portalfinance-prod` (IAM Identity Center SSO, region sa-east-1)"
  - "docs/ops/aws-copilot-setup.md runbook scaffold (section 0 populated, sections 1-7 stubbed)"
affects:
  - "Every later plan in Phase 01.1 that runs `copilot` or `aws` CLI commands"
tech-stack:
  added:
    - "AWS Copilot CLI"
    - "AWS IAM Identity Center"
  patterns:
    - "SSO-only AWS access (no long-lived IAM keys, per D-17)"
key-files:
  created:
    - docs/ops/aws-copilot-setup.md
  modified: []
key-decisions:
  - "Long-lived IAM access keys are forbidden for Copilot operations (D-17). Operator confirms `~/.aws/credentials` does not contain `aws_access_key_id` for `portalfinance-prod`."
  - "Permission set is named exactly `AdministratorAccess` so the assumed-role ARN matches `AWSReservedSSO_AdministratorAccess_*`. Verifier scripts grep for that prefix."
  - "Runbook structure mirrors `docs/ops/railway-setup.md` (sections 1-7) so subsequent Phase 01.1 plans can append in place."
requirements-completed:
  - OPS-01
duration: 5 min
completed: 2026-04-24
---

# Phase 01.1 Plan 00: Copilot CLI + IAM Identity Center SSO Prereq Summary

Provisioned local prerequisites for AWS Copilot CLI access to the Portal Finance AWS account in `sa-east-1`: Copilot CLI installed, IAM Identity Center SSO configured under profile `portalfinance-prod`, and a runbook scaffold committed for downstream plans to populate.

## Execution Stats

- Duration: 5 min
- Tasks: 2 (1 human-action checkpoint + 1 auto)
- Files: 1 created (`docs/ops/aws-copilot-setup.md`)

## Tasks Completed

| Task | Type | Status | Notes |
|------|------|--------|-------|
| 1. Install AWS Copilot CLI + configure IAM Identity Center SSO | checkpoint:human-action | Operator-confirmed | Operator verified Copilot CLI ≥ v1.34.1, `aws sts get-caller-identity --profile portalfinance-prod` returns assumed-role ARN, no `aws_access_key_id` for the profile in `~/.aws/credentials`. |
| 2. Scaffold `docs/ops/aws-copilot-setup.md` | auto | Done | Section 0 populated, sections 1-7 stubbed with "Populated by Plan 01.1-XX" markers. All 6 acceptance criteria pass. |

## Authentication Gates

- **AWS IAM Identity Center SSO bootstrap** — Operator interacted with the AWS Console (Identity Center setup, permission-set assignment) and the browser SSO flow to complete `aws configure sso` for profile `portalfinance-prod`. This is the canonical credential path for every later plan in Phase 01.1.

## Verification Results

Acceptance criteria for Task 2:

- `test -f docs/ops/aws-copilot-setup.md` -> PASS
- `grep "portalfinance-prod"` -> PASS
- `grep "IAM Identity Center"` -> PASS
- `grep "LGPD-05"` -> PASS
- `grep "D-17"` -> PASS
- `grep "## 0. Prerequisites (Wave 0 of Phase 01.1)"` -> PASS
- `grep "aws sso login --profile portalfinance-prod"` -> PASS
- Headings `## 1.` through `## 7.` -> all PASS

Acceptance criteria for Task 1 are operator-confirmed (browser/console + local CLI state on the developer laptop).

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0. **Impact:** none.

## Issues Encountered

None.

## Self-Check: PASSED

Ready for Plan 01.1-01 (package.json scripts + /api/health endpoint + next.config.ts), which depends on this plan.
