---
status: complete
phase: 01.1-infra-bootstrap-aws-sa-east-1-via-copilot
source:
  - 01.1-00-SUMMARY.md
  - 01.1-01-SUMMARY.md
  - 01.1-02-SUMMARY.md
  - 01.1-03-SUMMARY.md
  - 01.1-04-SUMMARY.md
  - 01.1-05-SUMMARY.md
  - 01.1-06-SUMMARY.md
  - 01.1-07-SUMMARY.md
  - 01.1-08-SUMMARY.md
started: 2026-04-30T00:00:00Z
updated: 2026-04-30T00:50:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Run `aws sso login --profile portalfinance-prod`, then force a fresh deploy
  of the web service: `copilot svc deploy --name web --env prod` (or
  `aws ecs update-service --force-new-deployment ...`). The new ECS task reaches
  RUNNING and the ALB target group transitions to healthy within ~90 s.
  CloudWatch logs for `/copilot/portalfinance-prod-web` show no entrypoint
  errors (no "DB_ENDPOINT is not set", no SSL/TLS failures, no "queue not
  found"). Immediately after, `curl -I https://portalfinance.app/api/health`
  returns HTTP/2 200.
result: pass

### 2. Local validation gates pass (SKIP_REMOTE=1)
expected: |
  From the repo root: `SKIP_REMOTE=1 sh scripts/validate-phase-01.1.sh`
  exits 0. Output shows [PASS] for every local gate (manifests present,
  RDS PubliclyAccessible: false, services in private subnets, migrate
  manual-only, CloudWatch retention 30, railway-setup.md ABSENT — the
  Plan 01.1-08 hardening). No [WARN] or [FAIL] lines. (PowerShell port
  `scripts/validate-phase-01.1.ps1 -SkipRemote` is byte-equivalent.)
result: pass

### 3. Remote validation gates pass (12 live gates)
expected: |
  With an active SSO session: `sh scripts/validate-phase-01.1.sh` exits 0
  against the live `prod` env. All 12 gates PASS, including: RDS
  PubliclyAccessible=false, CloudWatch retentionInDays=30 on every log
  group, 5 SSM SecureString parameters under
  `/copilot/portalfinance/prod/secrets/`, ACM cert ISSUED, edge 200 on
  apex AND www, `Server: cloudflare` header present.
result: pass

### 4. Public edge round-trip (apex + www)
expected: |
  `curl -I https://portalfinance.app/api/health` returns 200 with
  `server: cloudflare` and the JSON body `{"status":"ok"}`. Same for
  `https://www.portalfinance.app/api/health`. TLS chain validates against
  the ACM cert (apex + www SAN, ARN ending in `cf545575-...`). No
  mixed-content warnings or 5xx responses.
result: pass

### 5. Phase 1 schema applied on prod RDS (14 tables + pgcrypto)
expected: |
  Inside `copilot svc exec --name web --env prod`, run
  `/app/scripts/db-shell.sh -c '\dt public.*'` (in-image operator helper).
  Output lists all 14 Phase 1 tables: users, sessions, accounts_oauth,
  verification_tokens, user_consents, audit_log, admin_access_log,
  webhook_events, subscriptions, dsr_requests, auth_rate_limits,
  account_locks, password_reset_tokens, ses_suppressions. NO `accounts`
  table (Pluggy name reserved). `\dx` shows `pgcrypto` installed.
  `drizzle.__drizzle_migrations` table present with at least one applied row.
result: pass
note: |
  Verified via `/app/scripts/db-shell.sh "SELECT tablename FROM pg_tables
  WHERE schemaname='public' ORDER BY tablename"` returning all 14 tables.
  pgcrypto + drizzle.__drizzle_migrations confirmed via SQL probes
  (pg_extension lookup + COUNT on the migrations table).

### 6. Migration idempotence on prod (no-op re-run)
expected: |
  `copilot job run --name migrate --env prod` exits 0 and the migrate
  ECS task reaches STOPPED with exitCode 0. CloudWatch logs show only
  the three idempotent bootstrap NOTICEs (pgcrypto already exists,
  drizzle schema already exists, __drizzle_migrations already exists)
  followed by "migrations applied" — no per-migration log lines, no DDL
  emitted. A subsequent `\dt public.*` is byte-identical to Test 5.
result: pass

### 7. SSM SecureString secrets present (x5)
expected: |
  `aws ssm describe-parameters --parameter-filters "Key=Name,Option=BeginsWith,Values=/copilot/portalfinance/prod/secrets/" --profile portalfinance-prod --region sa-east-1`
  returns the 5 application secrets all with `Type: SecureString`:
  NEXTAUTH_SECRET, ENCRYPTION_KEY, CPF_HASH_PEPPER, SENTRY_DSN,
  TURNSTILE_SECRET_KEY. (Additional `String` entries DB_ENDPOINT/DB_NAME/
  DB_PORT/DB_SECRET_ARN are the DB-bridge metadata pattern from Plan
  01.1-05 — non-sensitive; the actual DB password lives in Secrets Manager.)
result: pass

### 8. Worker service running with pg-boss queues created
expected: |
  `copilot svc status --name worker --env prod` shows desiredCount=1,
  runningCount=1, lastStatus=RUNNING. CloudWatch logs for
  `/copilot/portalfinance-prod-worker` show pg-boss `boss.createQueue`
  log lines for every entry in QUEUES on startup (no "queue not found"
  errors). Worker stays up >5 minutes without restart.
result: pass

### 9. Railway runbook deleted + gate hardened
expected: |
  `docs/ops/railway-setup.md` does NOT exist (`test ! -f`).
  `scripts/validate-phase-01.1.sh` enforces a hard FAIL (not WARN) on
  presence — `grep -E "fail.*railway-setup\.md" scripts/validate-phase-01.1.sh`
  matches at least one line. Re-creating the file would cause the local
  gate to exit non-zero. PowerShell port enforces the same policy via
  `Test-FileAbsent` -> `Invoke-Fail` -> `exit 1`.
result: pass

### 10. Operations runbook is the single source of truth
expected: |
  `docs/ops/aws-copilot-setup.md` exists with sections 0-7 populated
  (`grep -E "^## [0-7]\." docs/ops/aws-copilot-setup.md` returns 8
  matches): SSO setup, three-workload topology, DB-bridge SSM, migration
  workflow, rollback, operational notes, manual steps.
  `docs/ops/encryption-key-rotation.md` references SSM + `copilot svc
  deploy` (not Railway dashboard). `docs/ops/ses-production-access.md`
  references workload addon `addons/ses-access.yml` + IAM task role
  (not static AWS keys).
result: pass

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
