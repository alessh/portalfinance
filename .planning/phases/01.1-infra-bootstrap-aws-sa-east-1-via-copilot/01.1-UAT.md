---
status: complete
phase: 01.1-infra-bootstrap-aws-sa-east-1-via-copilot
source:
  - 01.1-00-SUMMARY.md
  - 01.1-01-SUMMARY.md
  - 01.1-02-SUMMARY.md
  - 01.1-03-SUMMARY.md
  - 01.1-04-SUMMARY.md
  - 01.1-05-SUMMARY.md
  - 01.1-06-SUMMARY.md
  - 01.1-07-SUMMARY.md
  - 01.1-08-SUMMARY.md
started: 2026-04-30T00:00:00Z
updated: 2026-04-30T01:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Run `aws sso login --profile portalfinance-prod`, then re-deploy or
  force-restart the web task: `copilot svc deploy --name web --env prod`
  (or `aws ecs update-service --force-new-deployment ...`). The new ECS
  task reaches RUNNING and the ALB target group transitions to healthy
  within ~90 s. CloudWatch logs for `/copilot/portalfinance-prod-web`
  show no entrypoint errors (no "DB_ENDPOINT is not set", no SSL/TLS
  failures, no "queue not found"). A `curl -I https://portalfinance.app/api/health`
  immediately after returns HTTP/2 200.
result: pass

### 2. Local validation gates pass (SKIP_REMOTE=1)
expected: |
  From the repo root: `SKIP_REMOTE=1 sh scripts/validate-phase-01.1.sh`
  exits 0. Output shows [PASS] for every local gate (manifests present,
  RDS PubliclyAccessible: false, services in private subnets, migrate
  manual-only, CloudWatch retention 30, railway-setup.md ABSENT — the
  Plan 01.1-08 hardening). No [WARN] or [FAIL] lines.
result: pass
note: "Verified via the new PowerShell port `scripts/validate-phase-01.1.ps1 -SkipRemote` — 5 [PASS] lines + 'All local gates PASSED', exit 0. The bash version is byte-equivalent."

### 3. Remote validation gates pass (12 live gates)
expected: |
  With an active SSO session: `sh scripts/validate-phase-01.1.sh` exits
  0 against the live `prod` env. All 12 gates PASS, including: RDS
  PubliclyAccessible=false, CloudWatch retentionInDays=30 on every log
  group, 5 SSM SecureString parameters under
  /copilot/portalfinance/prod/secrets/, ACM cert ISSUED, edge 200 on
  apex AND www, `Server: cloudflare` header present.
result: pass
note: "Verified via the new PowerShell port `scripts/validate-phase-01.1.ps1`. Two PS 5.1 native-command quirks fixed during the run: (a) stderr-to-NativeCommandError wrapping breaks `& aws.exe 2>$file` under $ErrorActionPreference='Stop' — pivoted to Start-Process for full stream isolation; (b) parameter named `$Args` shadowed the PS automatic variable and dropped the array — renamed to `$Arguments`."

### 4. Public edge round-trip (apex + www)
expected: |
  `curl -I https://portalfinance.app/api/health` returns HTTP/2 200
  with `server: cloudflare` and the JSON body `{"status":"ok"}`.
  Same for `https://www.portalfinance.app/api/health`. TLS chain
  validates against the ACM cert (apex + www SAN, ARN ending in
  `cf545575-...`). No mixed-content or 5xx.
result: pass
note: "User confirmed via curl -vI: TLS handshake clean (ALPN http/1.1 negotiated), HTTP/1.1 200 OK on apex. HTTP/1.1 vs HTTP/2 is a Cloudflare ALPN choice, not a regression."

### 5. Phase 1 schema applied on prod RDS (14 tables + pgcrypto)
expected: |
  `copilot svc exec --name web --env prod --command "psql $DATABASE_URL -c '\\dt public.*'"`
  lists all 14 Phase 1 tables: users, sessions, accounts_oauth,
  verification_tokens, user_consents, audit_log, admin_access_log,
  webhook_events, subscriptions, dsr_requests, auth_rate_limits,
  account_locks, password_reset_tokens, ses_suppressions. NO `accounts`
  table (Pluggy name reserved). `\dx` shows `pgcrypto` installed.
  `__migrations` table present with at least one applied row.
result: pass
note: |
  Verified via `/app/scripts/db-shell.sh` (the in-image operator helper) inside
  `copilot svc exec --name web --env prod` — returned the 14 Phase 1 tables.
  Direct psql against `$DATABASE_URL` failed because exec shells do not inherit
  the composed URL by design (entrypoint shim scopes it to the CMD process tree
  and unsets DB_PASS afterwards). Image now also carries `postgresql-client` for
  future ad-hoc ops, though a `psql-shell.sh` credential-fetch wrapper would be
  needed for ergonomic use — tracked as a follow-up.

### 6. Migration idempotence on prod (no-op re-run)
expected: |
  `copilot job run --name migrate --env prod` exits 0 and the migrate
  ECS task reaches STOPPED with exitCode 0. CloudWatch logs show
  "no migrations to run" or equivalent — no DDL emitted, no schema
  drift. A subsequent `\dt public.*` is byte-identical to Test 5.
result: pass
note: |
  Job logs show three idempotent bootstrap NOTICEs (pgcrypto already
  exists, drizzle schema already exists, __drizzle_migrations already
  exists) followed by "migrations applied" with zero per-migration log
  lines — i.e. the migrator only re-asserted its own metadata scaffold
  and found no pending versions. Drizzle's tracking table is
  `drizzle.__drizzle_migrations`, not `public.__migrations` as Test 5's
  expected text described — wording bug, schema verification itself was
  correct.

### 7. SSM SecureString secrets present (x5)
expected: |
  `aws ssm describe-parameters --parameter-filters "Key=Name,Option=BeginsWith,Values=/copilot/portalfinance/prod/secrets/" --profile portalfinance-prod --region sa-east-1`
  returns exactly 5 entries, all with `Type: SecureString`:
  NEXTAUTH_SECRET, ENCRYPTION_KEY, CPF_HASH_PEPPER, SENTRY_DSN,
  TURNSTILE_SECRET_KEY. None are `String` (would be plaintext).
result: pass
note: |
  Pass on intent. The 5 expected secrets are SecureString. The 4 additional
  String entries (DB_ENDPOINT/DB_NAME/DB_PORT/DB_SECRET_ARN) are the DB-bridge
  pattern from Plan 01.1-05 — non-sensitive metadata + an ARN reference; the
  actual DB password lives in AWS Secrets Manager and never lands in SSM.
  Cosmetic gotcha: those 4 live under `.../secrets/` despite not being secrets,
  inherited from Copilot's DB-bridge convention. A future cleanup could move
  them to `.../dbinfo/` to make the path-name match its content. Tracked as a
  non-blocking follow-up.

### 8. Worker service running with pg-boss queues created
expected: |
  `copilot svc status --name worker --env prod` shows desiredCount=1,
  runningCount=1, lastStatus=RUNNING. CloudWatch logs for
  `/copilot/portalfinance-prod-worker` show pg-boss `boss.createQueue`
  log lines for every entry in QUEUES on startup (no "queue not found"
  errors). Worker stays up >5 minutes without restart.
result: pass

### 9. Railway runbook deleted + gate hardened
expected: |
  `docs/ops/railway-setup.md` does NOT exist (`test ! -f`).
  `scripts/validate-phase-01.1.sh` contains the FAIL-on-presence
  gate (not WARN) — `grep -E "fail.*railway-setup\\.md" scripts/validate-phase-01.1.sh`
  matches at least one line. Re-creating the file (touch + run script
  + delete) would cause the local gate to exit non-zero.
result: pass
note: |
  Verified directly: railway-setup.md ABSENT, bash script line 40 enforces
  hard FAIL via `[ ! -f ... ] || fail`, PS script line 141 enforces hard
  FAIL via `Test-FileAbsent` (which calls Invoke-Fail → exit 1). Both ports
  are byte-equivalent in policy.

### 10. Operations runbook is the single source of truth
expected: |
  `docs/ops/aws-copilot-setup.md` exists with sections 0 through 7
  populated (`grep -E "^## [0-7]\\." docs/ops/aws-copilot-setup.md`
  returns 8 matches). It covers: SSO setup, three-workload topology,
  DB-bridge SSM, migration workflow, rollback, operational notes,
  manual steps. `docs/ops/encryption-key-rotation.md` references SSM
  + `copilot svc deploy` (not Railway dashboard).
  `docs/ops/ses-production-access.md` references workload addon
  `addons/ses-access.yml` + IAM task role (not static AWS keys).
result: pass
note: |
  Verified directly. aws-copilot-setup.md present with all 8 headings 0..7.
  encryption-key-rotation.md: 17 SSM/copilot-deploy references, 0 Railway
  references — fully pivoted. ses-production-access.md: 4 task-role/
  ses-access references; the single AWS_ACCESS_KEY_ID mention is the
  SEC-02 enforcement line ("MUST stay unset in production"), not a
  recommendation to use static keys.

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
